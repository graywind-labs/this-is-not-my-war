# 《这不是我的战争》策划案 v0.1

## 0. 文档定位

本文档用于后续开发、参赛说明、Demo 范围控制和系统拆分参考。它不是最终宣传文案，而是偏执行层面的游戏设计文档。当前版本以“腾讯 AI 游戏比赛 Demo”为目标，优先保证核心 AI 交互、NPC 自主行为、征召入伍、资源经营和战斗防守形成闭环。

---

# 1. 项目概述

## 1.1 游戏名称

**《这不是我的战争》**

副标题暂定：**边境驿站守备日志** / **奉命守住这里**

## 1.2 类型定位

低多边形 3D 俯视视角模拟经营 + AI NPC 心理交互 + 小规模防守战斗。

玩家并不直接扮演一个可移动角色，而是在上帝视角下作为“边境驿站守备官”进行管理、发布指令、交涉、征召和防御部署。

## 1.3 核心卖点

玩家面对的不是传统意义上的“兵种单位”，而是一群本来不属于战争的人：马夫、厨子、园丁、铁匠、老兵副官、神父、医生、工程师。玩家需要通过说服、利诱、欺骗、威胁等方式，把这些拥有职业、恐惧、欲望、牵挂和记忆的 AI NPC 临时组织成防线。

NPC 会自动生活、工作、交流、记忆和反思。他们会记住玩家的承诺、欺骗、威胁、赠予、装备分配和伤害行为，也会在战后质问玩家，甚至逃离驿站。

## 1.4 核心体验关键词

- 战争压力下的临时动员
- AI 驱动的人格化 NPC
- 黑色幽默
- 小规模经营压力
- 玩家话术操纵与反噬
- “普通人变成战争资源”的荒诞感
- 主播友好的意外回复与系统翻车

## 1.5 一句话 Pitch

战争绕过前线逼近边境驿站，玩家作为临时守备官，必须用资源、话术、威胁和谎言，把一群马夫、厨子、医生、神父和工匠临时变成防线，并承受他们记忆中的反问与反噬。

---

# 2. 世界观与背景设定

## 2.1 时间与风格

背景大致参考中世纪晚期至文艺复兴早期的边境地带，氛围可借鉴西班牙收复失地运动时期的边境冲突感，但不直接绑定真实历史国家，以避免考据成本。

画面风格为低多边形 3D，俯视高机位，整体简洁、轻松、略带荒诞。场景表现不追求写实残酷，而是用简化可爱的低模外观承载黑色幽默和战争压力。

## 2.2 故事前提

玩家是一座边境驿站的临时守备官。这里本来不是主战场，因此原有卫兵大多已被调往前线。驿站只剩下玩家、副官和少量非战斗人员。

某日，驿站收到上级军令：敌人有一支部队绕过前线，正准备突袭此地。玩家必须守住驿站，确保主厅、仓库和交通节点不被摧毁，并等待后方反应。

上级给了玩家一批基础资源，但没有给足正规军。于是玩家只能将驿站中的普通人临时组织起来。

## 2.3 主题表达

游戏主题不是“英雄保卫家园”，而是：

> 当战争机器缺少士兵时，它会如何把厨子、马夫、医生、神父和工匠组织成“可用战力”。

玩家既是防御者，也是压迫者。玩家可以真诚地保护所有人，也可以通过谎言和恐惧维持秩序。游戏不强制道德审判，但让 NPC 记住玩家行为，并在系统层面产生后果。

---

# 3. 目标 Demo 范围

## 3.1 Demo 目标

Demo 版本目标是形成 30 分钟以上可体验内容，展示以下核心能力：

1. 8 名 AI NPC 根据人设、状态、记忆和场景自动制定日程。
2. 玩家可以与 NPC 自然语言对话。
3. 玩家可以通过对话让 NPC 同意应征入伍。
4. 入伍 NPC 可以接收玩家自由撰写的指令，并在自主计划、行动重评估和战斗判断中把指令作为重要参考；装备分配仍由玩家直接管理。
5. NPC 会记住承诺、欺骗、威胁、赠予、伤害、战斗经历和地点见闻。
6. NPC 在集结 / 战斗 / 避战对话、战斗中低血量和逃离挽留的玩家消息轮次中会触发 LLM 判定或结构化意向输出；逃离挽留里的攻击不触发 LLM 回复。
7. 驿站建筑、资源、生产、升级和防守形成基本闭环。
8. 防守 5 波敌人后胜利；主厅被摧毁或关键系统崩溃则失败。
9. 胜利/失败后生成所有 NPC 的结局与总结。

## 3.2 Demo 初始 NPC

Demo 初始共 8 名 NPC，对应 8 种职业背景：

1. 马夫
2. 厨子
3. 园丁
4. 铁匠
5. 老兵副官，女性，开局可控
6. 神父
7. 医生，女性
8. 工程师

职业不是硬锁身份，而是由初始熟练度、属性、人设和记忆共同决定。玩家后期可以让任意 NPC 做任意工作，只是效率、抵触程度和心理后果不同。

## 3.3 Demo 胜利目标

玩家需要在无尽波次中至少守住 5 波敌人攻击。第 5 波结束后进入 Demo 胜利结算。

## 3.4 Demo 失败条件

- 主厅被摧毁。
- 所有可战斗人员离开或处于昏迷状态，且下一波敌人无法抵抗。

---

# 4. 核心玩法循环

## 4.1 日常经营循环

```text
早晨：NPC 根据人设、记忆、状态、技能和地点信息制定今日计划
  ↓
白天：NPC 按 24 个阶段执行工作、吃饭、祈祷、聊天、治疗、训练、睡觉等行动
  ↓
玩家：观察资源、建筑、人物状态，与 NPC 对话，向已入伍者发布或修改指令，写公告，升级建筑，交易资源
  ↓
异常：行动失败、建筑被占用、资源不足、NPC 对话、玩家打断、战斗警报、逃离计划
  ↓
夜晚：NPC 在当前 21:00 锚定窗口内累计睡眠 1 个游戏小时后，总结短期记忆，更新知识图谱和第一人称日记
  ↓
第二天开始
```

## 4.2 征召与军事循环

```text
玩家观察 NPC 状态与弱点
  ↓
玩家与 NPC 对话：说服 / 利诱 / 欺骗 / 威胁 / 安抚 / 交易
  ↓
LLM 根据 NPC 人设、记忆、状态和当前话术生成回应与意向
  ↓
NPC 可能拒绝、接受
  ↓
NPC 同意应征后变成“可接收守备官指令的可引导单位”
  ↓
玩家发布自然语言指令，并分配武器、盔甲和坐骑
  ↓
战斗触发，NPC 根据行为模式、装备、战局、伤势、记忆和守备官话术进入集结、战斗、避战或逃离相关流程
```

## 4.3 战斗防守循环

```text
敌人来袭预告
  ↓
玩家摇响警铃，或敌人进入可接敌范围 / 攻击 NPC 或建筑
  ↓
入伍且有武器的 NPC 进入集结模式，前往城门外防线
  ↓
集结途中或集合点接敌后进入战斗模式
  ↓
敌人攻击城门、仓库、主厅或附近单位；当前规则不把围墙作为攻击目标
  ↓
NPC 根据装备、熟练度、战斗策略和状态自动战斗
  ↓
玩家可在集结 / 战斗 / 避战中发起公开对话，影响斗志激昂或逃离
  ↓
战时任一 NPC HP 首次低于 30% 时触发自身心理判定；避战 NPC 也会判定
  ↓
血量清零后倒地昏迷，等待自然恢复或其他NPC治疗
  ↓
战斗结束，战场事件进入广场见闻系统
```

## 4.4 NPC 行为模式总览

NPC 运行时至少区分四种同级行为模式：工作模式、集结模式、战斗模式、避战模式。模式是程序权威状态，不由 LLM 直接改写；LLM 只能通过对话或判定输出意向，再由程序校验并应用。

| 模式 | 适用 NPC | 核心行为 | 进入条件 | 退出 / 切换条件 |
|---|---|---|---|---|
| 工作模式 / 日常模式 | 所有未昏迷、未逃离 NPC | 按计划工作、吃饭、睡觉、训练、治疗、对话、计划异常重评估 | 默认状态；战斗、集结或避战结束后返回 | 警铃集结、接敌、非战斗人员遇敌避战、昏迷、逃离等高优先级事件会打断 |
| 集结模式 | 已入伍、有主武器、当前可行动 NPC | 前往城门外防线，近战 / 骑兵前排，远程 / 骑射后排，等待接敌 | 守备官摇响警铃 | 集结途中或集合点敌人进入一定范围，进入战斗模式；到达集合点后等待 1 游戏小时仍未接敌，返回工作模式并按当前计划继续，不触发计划重评估 |
| 战斗模式 | 已入伍且有主武器的可战斗 NPC | 按自身兵种与战斗策略攻击、保持距离、护卫或执行战斗内避战策略 | 从集结模式接敌；或工作模式中敌人进入一定范围；正在睡觉的可战斗 NPC 只有被敌人攻击才从日常直接进入战斗 | 场上敌人全部消失后退出战斗，返回工作模式并重新评估计划；HP 清零进入昏迷 |
| 避战模式 | 非战斗人员：未入伍 NPC，或已入伍但无主武器 NPC | 根据敌人接近方位逐步远离敌人，尝试退到接敌范围之外；不离开驿站太远；不攻击敌人 | 非战斗人员工作模式下敌人进入一定范围；正在睡觉的非战斗人员只有被敌人攻击才进入避战 | 场上敌人全部消失后返回工作模式；避战中若应征但仍无主武器，继续避战；装备主武器且场上仍有敌人时进入战斗模式 |

避战模式不同于战斗模式中的“避战策略”。非战斗人员的避战模式与战斗模式同级，代表没有武装接战能力的人躲避袭击但仍留在驿站；已入伍且有主武器 NPC 的“避战”只是战斗模式中的可选战术策略，仍属于战斗模式，不等于逃离驿站。

任一高优先级模式触发时，如果 NPC 正在与守备官对话，则系统强制完成当前已发生的会话、关闭对话框并取消该 NPC 当前可取消的 LLM 回复请求；尚未返回的 NPC 回复不入库，但已经显示的守备官消息和已发生的攻击事实仍按完成会话保存并进入计划修改判别。若 NPC 正在执行普通计划行动、移动、工作、吃饭、训练、治疗或计划 LLM 活动，系统中断该活动并进入新模式。首次睡眠总结锁仍是更高优先级的不可打断状态，但睡觉 NPC 被敌人直接攻击时会按上表从睡觉进入战斗或避战。

昏迷 NPC 复苏后按场上敌军、入伍状态和主武器重新分流：若仍有敌军，已入伍且有主武器者进入战斗模式，未入伍或无主武器者进入避战模式；若场上无敌军，返回工作模式并重新评估计划。

信息记录上，工作模式与战斗模式之间、工作模式与避战模式之间的切换本身不写入事件库，也不作为地点公开广播；避战开始 / 结束、攻击、受伤、警铃、集结、集结接敌、昏迷和复苏等具体事实仍由各自事件记录。

---

# 5. 玩家身份与交互方式

## 5.1 玩家身份

玩家是边境驿站守备官，但游戏中没有可移动玩家角色。玩家始终以上帝视角进行交互，类似 RTS 或模拟经营游戏中的管理者。

所有面向 NPC 记忆、见闻、对话 Prompt、首次睡眠总结、教学信件和游戏内叙事文本的玩家称呼，统一使用“守备官”。“玩家”只用于设计文档、开发文档和调试说明中的外部说明，不应出现在 NPC 会看到或 LLM 会当作世界内事实理解的事件摘要里。

玩家的权力包括：

- 资源分配权
- 警铃与军事调度权
- 公告发布权
- 对已入伍者发布和修改自然语言指令的权力
- 对非入伍者的劝说、利诱、欺骗、威胁能力

## 5.2 玩家主要操作

- 查看 NPC 状态、当天事件 / 见闻、第一人称日记、知识图谱和当前行动
- 与 NPC 对话
- 请求 NPC 应征入伍
- 向已入伍 NPC 发布工作、训练、休息、防守等自然语言指令
- 给 NPC 装备武器、盔甲、坐骑
- 赠予 NPC 金钱或资源
- 在对话窗中通过攻击按钮对 NPC 造成伤害，以形成威胁或惩罚，并让 NPC 对惩戒攻击作出回应
- 升级和修复建筑
- 部署工程器械
- 管理资源
- 与商人交易
- 在主厅前公告牌界面编辑通告和通用参考日程
- 摇响警铃进入战斗准备
- 加速时间
- 在 NPC 逃离时进行挽留或威胁

## 5.3 非对话交互记录

玩家与 NPC 的交互不只包括对话。以下行为也会作为事件写入目标 NPC 的短期记忆；若事件具有公开性，则根据见证范围即时广播给同地点/广场中已经在场的 NPC，并写入接收者的见闻库：

- 给予金钱
- 赠予酒
- 给予或更换装备
- 给予坐骑
- 发布或修改指令
- 在指令中要求工作、训练、休息或治疗
- 使用对话窗攻击按钮造成伤害
- 在逃离过程中威胁或伤害 NPC

这些事件会影响 NPC 后续的对话、计划、征召意愿、战斗判定和首次睡眠反思。

---

# 6. 地图与建筑系统

所有需要成员投入的工作、照料、训练、治疗和制造活动，都只有在成员持续合法参与时才推进，并在完成相应周期或到达程序定义的结算点后产生资源、制造进度、恢复或成长。成员离岗、改做其他事情或被高优先级事件打断后，原活动不会在无人持续参与时自行继续产生结果。未完成进度是否保留由具体系统决定，不能由 LLM 假定为全部归零或自动延续。

## 6.1 地图结构

驿站地图为一个封闭据点，主要由室外公共区域和多个建筑组成。室外广场和可进入建筑都视为地点/建筑信息节点：它们负责保存当前状态并转运公开信息，但不保存事件历史。

地点分为两类：

- 可进入地点：广场、宿舍、食堂、酒窖、菜园、铁匠铺、训练场、马厩、小教堂、小诊所、工械坊等。NPC 可以进入这些地点；地点内部发生的公开事件即时广播给当时已经在场的 NPC，地点本身不保存事件。
- 不可进入实体：主厅、围墙、城门、后门、仓库等。它们仍有建筑状态和可被攻击/修复/升级/摧毁的程序数据，但不作为 NPC 常规进入地点。它们的可传播外部状态会作为广场进入事件和广场地点状态的一部分被读取。

所有室外事件默认归属广场。广场没有建筑 HP，但会保存室外人员、室外人员的生命/行动状态、公告牌当前已发布通告、通用参考日程及其非强制备注，以及所有建筑的可传播外部状态。NPC 进入广场时只在见闻库中获得人员、人员状态和建筑外部状态，不重复获得公告牌两页；通告 / 参考日程在守备官发布实际变更时按页面分别向全站可接收见闻的 NPC 广播一次。战斗等其他公开事件继续通过广场即时广播给当时在广场的 NPC。

地图关键通道：

- 正门：敌人主要进攻方向
- 后门：商人马车到达与 NPC 逃离路线
- 围墙：当前作为防御边界和工程器械平台，不作为规则敌人的直接攻击目标
- 主厅：核心建筑，被摧毁则失败

## 6.2 建筑列表

### 6.2.1 主厅

象征玩家所在地和驿站指挥中枢。不可进入或不作为常规可操作建筑。敌人最终攻击目标。主厅被摧毁则游戏失败。

### 6.2.2 宿舍

始终有 10 个“床位”，不随升级增加。8 名初始 NPC 按叙事到站顺序固定使用床位 1–8：艾达、托马、布鲁诺、伊沃、格伦、欧文、马塞尔、莉娜；床位 9–10 保留为空余床位。NPC 执行“睡觉”时由程序自动申请自己的固定床位，不让 NPC 或玩家选择编号，也不会在每次入睡时重新随机或按当次空闲顺序分配。未来没有固定床位的 NPC 只能申请未分配床位。宿舍升级不增加床位，但可以提高单位逻辑时间内的疲劳恢复效率。固定归属只用于程序申请规则，建筑面板不显示“XX 专属”；玩家只看到 `床位X：空闲` 或 `床位X：XX占用中`。

### 6.2.3 食堂

初始有 1 个“灶台”和始终 10 个“用餐席”。“加工餐食”占用灶台，“吃饭”占用用餐席；同类位置由程序自动分配。食堂升级可增加灶台和餐食加工效率，但用餐席固定为 10 个；升级还可提高进食期间的饱食度恢复效率。厨艺熟练度继续影响餐食加工效率。

### 6.2.4 酒窖

消耗粮食酿酒。酒可以出售换取金钱。

### 6.2.5 菜园

由 NPC 工作，产出粮食。耕种熟练度、建筑等级影响产出。

### 6.2.6 铁匠铺

消耗铁为主、少量木材为辅，制作金属武器与护甲。当前 Demo 的闭合制造目录为剑盾、长杆武器、铁盔、锁子甲、铁护腕、铁护腿；不能用一份笼统“武器”或“盔甲”库存替代任意具体物品。打铁熟练度、力量和建筑等级影响每个工作周期的耗时，物品难度由需要完成的阶段数表达。当前不做耐久和品质随机词条。

### 6.2.7 训练场

用于训练军事熟练度。初始有 1 个“教官位”和 2 个“训练位”，后续升级可以增加任一类位置。“指导训练”占用教官位，“接受武器训练”占用训练位。接受训练在开始时必须有至少一名有效在岗教官；没有教官时立即失败并触发计划重评估。训练周期内若全部教官退出，当前所有受训行动立即中断、释放训练位并按“教官离岗”失败进入计划重评估；已累计但尚未结算的本周期进度不继续推进。

训练项目不由 UI 任意选择，而由 NPC 当前装备决定。NPC 当前持有主武器时，训练对应武器熟练度；当前拥有坐骑时，训练骑术；同时拥有武器和坐骑时，两项都会提升。没有武器且没有坐骑的 NPC 既不能当教官，也不能作为受训者进入训练。

教官没有受训者时，会以极慢速度练习自己当前持有的武器或坐骑，提升自己的剑盾、长杆、弓、弩或骑术。有受训者时，当前所有在岗教官共同组成指导团队：程序汇总在岗人数、每人“教练”熟练度与其对应训练项目的熟练度，得到同一个团队效率倍率，并应用到所有当前训练位上的受训者，不建立人工的教官—受训者绑定。教官有受训者时不再提升自己的武器或骑术，而是提升职业熟练度“教练”；受训者提升哪一项取决于受训者自己的当前武器和坐骑。教官当前拿什么武器只影响其独自练习项目，不限制受训者选择项目。

训练按单位时间结算，持续消耗疲劳和饱食。最终训练速率统一由基准速率、指导团队、建筑升级效率和建筑损伤效率相乘得到，同一个倍率不在“周期缩短”和“单位周期收益增加”两处重复计算。

### 6.2.8 马厩

用于马匹喂养、恢复、繁育和成长。新游戏初始有两匹不同 ID、刚达到 60% 成年阈值但尚未完全成长的马；马匹是有基础 HP、照料额外 HP、饱食度、成长、繁育概率和分配关系的个体，不再用“马匹整备”通用库存代表。养马熟练度影响繁育概率累积、幼马成长速度和可培养的额外 HP；马匹战斗受伤仍由后续战斗任务处理。

### 6.2.9 小教堂

始终有 1 个“祭坛”和 10 个“祈祷席”，两类位置都不随升级增加。教堂活动明确拆为三种行动：普通“祈祷”和“参加弥撒”都占用祈祷席，“主持弥撒”占用祭坛。

普通祈祷不依赖神父或其他服务者，神父离开驿站后仍可进行；但祭坛上正在主持弥撒时，新的普通祈祷会失败，弥撒开始也会中断已经进行中的普通祈祷并释放祈祷席。“参加弥撒”只能在一名具备资格的 NPC 正在祭坛主持时开始，并绑定该次主持：主持正常完成时，参加者一并完成；主持异常中断或主持者离岗时，参加者失败并释放祈祷席。只有具备配置能力“主持弥撒”的 NPC 能执行主持弥撒；所有 NPC 都可看到主持选项，不具备能力者同时看到“你没有主持弥撒的能力”的资格提示，若仍尝试执行则由程序拒绝并触发计划重评估。普通祈祷、主持和参加都必须在真正到达小教堂且已经结束移动后才能开始；程序不能因为 `current_location` 还残留旧值，就让位于广场或仍在路上的 NPC 隔空祈祷。

### 6.2.10 小诊所

初始有 1 个“诊疗位”和 2 个“病床”，后续升级可以增加任一类位置。“坐诊 / 研读医学书籍”占用诊疗位，“接受诊所治疗”占用病床。当前所有在岗医生共同组成诊疗团队：程序汇总在岗人数、每人医术熟练度及配置的相关属性，得到同一个团队效率倍率，并应用到所有当前病床上的病人，不建立人工的医生—病人绑定。接受诊所治疗在开始时必须有至少一名有效在岗医生；没有医生时立即失败。治疗周期内若全部医生退出，当前所有病床治疗立即中断、释放病床并按“医生离岗”失败进入计划重评估。治疗消耗金钱，最终恢复速率由基准速率、诊疗团队、建筑升级效率和建筑损伤效率相乘得到。NPC 昏迷后可以在原地缓慢恢复，也可以由其他 NPC 治疗以更快恢复生命值；当生命值恢复到 30% 时，NPC 会从昏迷中复苏。

### 6.2.11 工械坊

在此消耗木材为主、少量铁为辅，制造木质武器、箭矢与防御器械。当前 Demo 的闭合制造目录为弓、弩、箭束、弩床、箭塔；“简易塔防装置等”是类别描述，不额外生成一个没有定义、无法部署的通用物品。每件成品都保存独立库存，弩床与箭塔部署时只消耗同名器械。

### 6.2.12 仓库

仓库承担全站金钱、粮食、木材、石料、铁、武器和装备的集中登记。当前已实现的规则是：敌人攻破正门后先攻击仓库，再攻击主厅；修复与升级提高仓库生命值；仓库等级限制粮食、餐食、酒、木材、石料、铁的储存上限，1 级分别为 120、120、60、120、120、120，后续每级分别增加 60、60、30、60、60、60。工作和购买在入库前做原子容量检查，仓库面板及 HUD 悬停显示当前上限。敌人攻击仓库造成资源流失、仓库等级影响被攻击损失比例仍是待实现目标，在战斗结算与验证闭环完成前不得由 UI 或 LLM 表述为当前生效规则。

### 6.2.13 围墙

敌人攻入驿站前需要先破坏城门。围墙有生命值，可消耗石料修复或升级，当前规则敌人不直接攻击围墙；后续可通过碰撞体积与导航让 NPC / 敌人不能直接穿越围墙。工程器械可部署在围墙配置槽位上：弩床以较高单次伤害攻击射界内敌人，箭塔以较短间隔持续射击。部署由玩家直接操作；弩床只消耗 `item_wall_ballista`，箭塔只消耗 `item_wall_arrow_tower`，不要求 NPC 参与；具体伤害、间隔、射程、槽位与表现资源均读取配置。

### 6.2.14 公告输入接口（非建筑）

主厅前显示公告牌模型，但它不属于建筑系统，不拥有 HP、等级、工作位、修复或升级等建筑数据。公告牌包含“通告 / 日程表”两个玩家页签：通告页以当前已发布文本为编辑草稿，退出不保存，只有发布才更新；日程表由开始时间、结束时间和内容组成，支持新增、编辑与删除，同样只在发布后生效。程序内部仍可使用 `reference_schedule` 表达它不替代 NPC 个人计划的性质。

新游戏预置守备官口吻的战事通告，以及覆盖睡眠、三餐、通用工作、每日一次弥撒、休息 / 祈祷的参考日程。日程表始终注明“这是建议日程，驿站成员不一定严格按照这个日程，如有特殊事务可自行安排”，不得替代 NPC 自己的计划。通告和日程都作为广场当前状态保存；守备官发布发生实际变化的通告时向全站可接收见闻的 NPC 广播一次 `plaza_notice_changed`，发布发生实际变化的参考日程时独立广播一次 `plaza_schedule_changed`。只改一页不广播另一页，两页都改并分别发布时各广播一次，相同内容不重复广播。NPC 后来进入广场不会再从入场快照获得或重复记录这两页内容。初始两页内容预先写入全部初始在站 NPC 的见闻库。通告可用于宣传、谎言、命令、安抚或威胁。

## 6.3 建筑通用属性

| 属性 | 说明 |
|---|---|
| 建筑等级 | 影响产出、容量、效率或防御能力 |
| 建筑生命值 | 被攻击时下降，归零后失效或摧毁；属于权威数值和 UI 状态，不作为 NPC 信息传播的状态差异 |
| 完好/受损/正在修复/正在升级 | NPC 信息传播使用的建筑外部状态口径 |
| 功能位置 | 每个可进入建筑的位置类型、显示名、数量、占用者和是否可随升级扩展 |
| 运行效率 | 建筑等级奖励、损伤折减和服务团队加成的权威合成结果；玩家看精确值，NPC 信息只看分档 |
| 信息节点 | 保存建筑当前状态并向在场 NPC 广播公开事件和必要的字段级状态变化，不保存事件历史 |

### 6.3.1 功能位置合同

“主动 / 被动工位”只是设计时的分类概念，不得出现在玩家界面、NPC 见闻或世界内文案中。对外始终显示具体的位置名。

| 建筑 | 位置 `type` | 玩家显示名 | 行动 | 初始数 | 容量规则 |
|---|---|---|---|---:|---|
| 小教堂 | `chapel_altar` | 祭坛 | `lead_mass` / 主持弥撒 | 1 | 固定 |
| 小教堂 | `chapel_prayer_seat` | 祈祷席 | `pray_at_chapel` / 祈祷；`attend_mass` / 参加弥撒 | 10 | 固定 |
| 小诊所 | `clinic_doctor_station` | 诊疗位 | `work_clinic_doctor` / 坐诊或研读医学书籍 | 1 | 可升级扩展 |
| 小诊所 | `clinic_patient_bed` | 病床 | `receive_clinic_treatment` / 接受诊所治疗 | 2 | 可升级扩展 |
| 训练场 | `training_instructor_station` | 教官位 | `work_training_instructor` / 指导训练 | 1 | 可升级扩展 |
| 训练场 | `training_practice_slot` | 训练位 | `receive_weapon_training` / 接受武器训练 | 2 | 可升级扩展 |
| 食堂 | `dining_kitchen_station` | 灶台 | `work_dining_hall` / 加工餐食 | 1 | 可升级扩展 |
| 食堂 | `dining_seat` | 用餐席 | `eat_at_dining_hall` / 吃饭 | 10 | 固定 |
| 宿舍 | `dormitory_bed` | 床位 | `sleep_in_dormitory` / 睡觉 | 10 | 固定 |

每种位置必须至少映射一个可选行动。计划项和 NPC 只选择行动及目标建筑，不选择“1 号床”或其他具体编号；执行时由程序原子地分配一个同类空闲位置。同类位置已满、建筑正在升级、建筑已失效或行动资格不符时，申请必须失败且不抢占任何位置，随后进入统一计划重评估链路。完成、失败、中断、离开或转入高优先级状态时都必须释放位置。

诊所和训练场的多人团队效率是程序权威数值。它由当前有效占用诊疗位 / 教官位的全部 NPC 的人数和相应熟练度汇总，按建筑配置的加成、上限和递减规则得出，不由 UI 或 LLM 计算。所有当前病床 / 训练位共享团队倍率；人员上下岗时实时重算，已累计进度不回滚。依赖行动开始前必须已有有效在岗服务者；同批计划中服务者仍在前往工位时，依赖者可以等待其到岗。服务周期开始后若有效服务者归零，则立即失败，而不是等待途中替补。

## 6.4 建筑修复与升级

所有建筑都可以被修复。建筑受损后可消耗石料、木材等资源修复。玩家点击修复时一次性支付资源，但建筑不会瞬间回满 HP；系统会创建修复倒计时，并在倒计时推进过程中按比例逐步恢复 HP。修复总时长由缺失 HP 和建筑等级决定：缺失 HP 越多、建筑等级越高，耗时越长。修复开始状态必须同时传播本次总工期的“小时 / 分 / 秒”可读文本，使 NPC 制定或修订计划时知道工程量级；持续变化的剩余时间只在请求构造时实时读取，不按秒写入见闻。NPC 可在自主计划中选择协助正在进行的建筑修复；守备官也可在已入伍 NPC 的自然语言指令中要求其协助，但指令只作为计划和判断参考，不直接启动行动。协助修复发生在广场/室外，事件 `location_id` 为 `plaza` 且 `visibility` 为 `local_public`。NPC 如果已经在广场可直接协助；如果在室内，则需要先到广场再协助。协助期间根据其工程熟练度为修复倒计时提供小额加速，多个 NPC 可以同时协助并叠加加速。NPC 只有仍在广场且当前行为仍是协助该建筑修复时才提供加成，一旦离开广场或计划改变，协助人数和加成立即移除。

所有建筑都有升级潜力。建筑升级也主要消耗石料，但升级不再瞬间完成：玩家开始升级时一次性支付配置资源，系统创建配置时长的升级倒计时，并随开始状态传播本次总工期的“小时 / 分 / 秒”可读文本；倒计时结束后才提升等级并应用 Max HP、功能位置增量和效率奖励。除了配置明确无位置或某类位置固定的建筑，每级都可以配置增加一种或多种位置、提高产出 / 恢复 / 训练效率及提高 Max HP，具体奖励不得写死在 UI。

建筑升级缓慢推进、成员可协助加快是所有 NPC 都知道的驿站基础常识；但“当前哪座建筑正在升级、能否协助”必须以本次动态行动候选为准。规则本身不能让 LLM 自造升级作业或完成事实。

升级从启动到完成期间，建筑处于封闭状态：NPC 不能进入，所有内部位置不可申请或继续使用。升级启动时，程序必须让已经在建筑内执行依赖活动的行动立即以“建筑升级”为权威原因失败，释放所有位置，并将建筑内原占用者转移到广场逻辑地点；仍在前往该建筑的 pending 行动继续移动，NPC 真正到达入口、程序确认无法进入后才以同一权威原因失败并进入统一的行动失败计划重评估链路，不能让 NPC 在另一座建筑时隔空知道升级。没有依赖行动所有权的普通访问者只在入口被拒并收束到广场，不伪造计划失败。建筑受损时不可升级，正在修复时不可升级，正在升级时不可修复；只有完好且未处于修复 / 升级作业中的建筑才能开始升级。NPC 可在自主计划中选择协助正在进行的建筑升级；守备官也可通过已入伍 NPC 的当前指令表达这一要求，但不直接强制启动行动。协助升级发生在广场 / 室外，不需要进入封闭建筑，并作为有效劳动计入日计划工作阶段；事件 `location_id` 为 `plaza` 且 `visibility` 为 `local_public`。

建筑受损后不是只改变 HP 显示。程序必须按配置的单调效率曲线，由 `hp / max_hp` 得到 `condition_efficiency`：受损越严重，产出、加工、诊疗、训练、进食和睡眠恢复的有效速率越低；HP 归零或正在升级时效率为 0 且建筑不可用。统一以“每逻辑秒有效进度 / 恢复量”应用该倍率，不在周期长度和周期收益两端重复乘算。

升级方向示例：

- 食堂：增加加工效率
- 宿舍：轻微增加疲劳恢复效率
- 酒窖：提高酿酒效率
- 菜园：提高粮食产出
- 铁匠铺：解锁更高级装备或提高制作速度
- 训练场：提高训练效率，增加教官位或训练位
- 马厩：增加马匹恢复和再生产效率
- 小诊所：增加诊疗位或病床，提高治疗效率
- 工械坊：解锁弩床、箭塔等器械
- 仓库：提高生命值与六项大宗资源容量；降低被掠夺比例仍是待实现目标
- 围墙：提高生命值、防御值或部署槽位

## 6.5 铁匠铺与工械坊的制造项目

铁匠铺和工械坊各自只有一个建筑级“当前制造项目”。玩家必须先在该建筑面板选择合法目标，NPC 的对应工作才具备开始条件。配方、阶段名称、逐阶段材料和成品库存 ID 全部来自配置，UI、LLM 和 NPC 自身不能直接改写阶段或产出。

当前配方目录与难度如下。每一行的材料总量会拆分到配置中的各阶段；材料在对应阶段完整完成时原子扣除，阶段中断不扣除该阶段材料。已经完成阶段消耗的材料不会因换目标返还。

| 建筑 | 目标 | 具体库存 ID | 阶段数 | 材料总量 |
|---|---|---|---:|---|
| 铁匠铺 | 铁盔 | `item_iron_helmet` | 2 | 铁 2 |
| 铁匠铺 | 铁护腕 | `item_iron_bracers` | 2 | 铁 2 |
| 铁匠铺 | 长杆武器 | `item_polearm` | 3 | 铁 2、木材 1 |
| 铁匠铺 | 铁护腿 | `item_iron_greaves` | 3 | 铁 3 |
| 铁匠铺 | 剑盾 | `item_sword_shield` | 4 | 铁 3、木材 1 |
| 铁匠铺 | 锁子甲 | `item_mail_chest` | 6 | 铁 6 |
| 工械坊 | 箭束 | `item_arrow_bundle` | 1 | 木材 1 |
| 工械坊 | 弓 | `item_bow` | 2 | 木材 2 |
| 工械坊 | 弩 | `item_crossbow` | 4 | 木材 3、铁 1 |
| 工械坊 | 弩床 | `item_wall_ballista` | 6 | 木材 5、铁 1 |
| 工械坊 | 箭塔 | `item_wall_arrow_tower` | 8 | 木材 7、铁 1 |

阶段规则：

- 一个 NPC 完整完成一个对应工作周期，只能提交一个阶段。阶段提交时再次校验目标版本和该阶段材料，避免换目标或资源被其他系统花掉后错误产出。
- 工作周期被对话、改派、战斗、昏迷、离开工位或其他高优先级状态打断时，该 NPC 当前周期的部分进度归零；建筑已经完成的整数阶段不回退。
- 同一建筑升级后若有多个合法工位，多个 NPC 可同时进行周期；每个未完成周期只是待提交贡献，完成时由系统依次领取下一未完成阶段，不会重复完成同一阶段或重复产出。
- 在建筑面板换目标会放弃旧项目全部整数阶段并中断仍在进行的旧目标周期。只要“已完成阶段 + 当前进行中周期部分进度”大于 0，UI 必须弹出确认；只有点击“是”才提交更换，取消后下拉框恢复原目标。
- 全部阶段完成后，系统只向对应具体库存增加 1 件成品，然后保留当前目标并把该项目阶段重置为 0，便于后续继续制造同类物品。
- 箭束当前只作为具体库存，不在本任务擅自增加弓 / 弩弹药消耗；弹药战斗规则需要单独任务。

制造项目是可进入建筑的内部特殊状态。铁匠铺 / 工械坊的状态只暴露当前目标、目标名称、已完成阶段、总阶段、当前阶段序号和阶段名称；NPC 进入该建筑时在进入快照中得知，在场且未昏迷 / 未睡觉的 NPC 只在这些字段变化时收到字段级差量。制造状态不是建筑外部状态，不进入广场的全建筑外部状态，也不向不在该建筑的 NPC 广播。面板可以额外读取当前工作周期的小数进度用于进度条，但该逐秒小数不作为 NPC 见闻广播，避免短期记忆刷屏。

## 6.6 马匹个体与马厩运行

每匹马至少保存：唯一 ID、名字、HP、自然 HP 上限、养马额外 HP 上限与当前额外 HP、饱食度、饱食度上限、成长值、是否成年、当前位置、进食状态、分配 NPC。配置提供初始马和所有平衡值，不把个体数据写死在脚本中。

当前 Demo 基准参数：

| 参数 | 基准值 |
|---|---:|
| 初始刚成年马 | 2 匹（栗风、灰鬃），成长 60% |
| 马厩内饱食消耗 | 每游戏小时 0.5 |
| 马厩外饱食消耗 | 每游戏小时 2.0 |
| 触发进食缺口 | 饱食上限的 20% |
| 单次进食周期 | 1200 游戏秒 |
| 单次进食消耗 / 恢复 | 粮食 1 / 饱食 35 |
| 自然 HP 恢复 | 每游戏小时 0.2 HP |
| 自然恢复的饱食消耗 | 每恢复 1 HP 消耗 0.5 饱食 |
| 新生幼马自然 HP / 饱食上限 | 40 / 50 |
| 完全成长自然 HP / 饱食上限 | 100 / 100 |
| 成年阈值 | 成长度 60% |
| 基础完全成长时间 | 10080 个有效照料游戏分钟 |
| 每有效照料分钟基础繁育概率增量 | 0.005%，再乘养马能力与马厩等级 |
| 繁育概率上限 | 100% |
| 繁育成功冷却 | 1440 游戏分钟 |
| 养马 100 的额外 HP 上限 | 20 |

饱食与恢复规则：

- 马在马厩内缓慢消耗饱食，离开马厩后按更快速度消耗。只有物理上处于马厩、饱食缺口达到阈值且当前未进食的马，才会自行开始进食。
- 进食和 NPC 吃饭一样是持续行动：周期结束时才检查并原子消耗粮食、恢复饱食；粮食不足时不产生伪完成，马保持饥饿并等待后续粮食。
- 受伤马匹在非战斗结算中可非常缓慢地消耗自身饱食恢复 HP，但自动恢复最多到当前自然 HP 上限。高于自然上限的养马额外 HP 受损后不会靠自动恢复补回，只能在之后有养马人工作时重新培养。

繁育、成长和额外 HP 规则：

- 只有马厩中至少有一名正在执行有效 `work_stable` 的 NPC 时，繁育概率、幼马成长和额外 HP 培养才推进；本轮使用在岗养马熟练度最高者作为有效养马能力，避免多个工位重复叠加。
- 每匹物理上在马厩、达到成年阈值且不在繁育冷却中的马独立保存繁育概率。至少有 2 匹符合条件时，每个完整有效照料分钟都让各马的概率从 0 按“基础增量 × 养马能力 × 马厩等级”增长，再按当前累积概率执行最多 `N - 1` 次判定；单分钟最多出生 1 匹幼马。
- 繁育成功后，本次全部符合条件的成年在厩马繁育概率归零并进入 1440 游戏分钟冷却；冷却随逻辑时间下降，期间概率保持 0，但成长和额外 HP 培养仍可继续。
- 新生马从成长度 0 开始。只有有效养马人在岗时成长；自然 HP / 饱食上限随成长从幼马值线性提高到完全成长值。达到 60% 即算成年，可参与分配和繁育，但仍会继续成长到 100%。
- 养马能力同时决定额外 HP 可培养上限。该上限是持久的训练 / 照料成果，不因养马人离岗瞬间下降；额外 HP 只能通过有效照料慢慢补充，不属于自然恢复。

马厩的信息节点内部特殊状态只保存物理上仍在马厩的 `total / adult / foal` 三个数量。NPC 不通过建筑状态获得马名、个体 HP、饱食、成长、进食或分配详情；玩家的马厩建筑面板可以从马匹权威系统逐匹读取这些信息。数量进入快照和字段级差量的传播边界与 6.5 相同，不公开到广场。

---

# 7. 资源系统

## 7.1 主要资源

| 资源 | 暂定名称 | 来源 | 用途 |
|---|---|---|---|
| 金钱 | 第纳尔 | 商人交易、出售酒/物资、上级补给 | 购买资源、治疗、利诱 NPC |
| 粮食 | 粮食 | 菜园、商人购买、初始补给 | 吃饭、酿酒、喂马 |
| 木材 | 木材 | 商人购买、初始补给 | 工械坊制造、建筑升级 |
| 石料 | 石料 | 商人购买、初始补给 | 修复和升级建筑 |
| 铁 | 铁 | 商人购买、初始补给 | 铁匠铺制作武器盔甲 |

## 7.2 派生资源

| 派生资源 | 来源 | 用途 |
|---|---|---|
| 餐食 | 食堂加工粮食 | 更高效恢复饱食度 |
| 酒 | 酒窖消耗粮食酿造 | 出售，或由守备官赠予 NPC 后供其个人饮用 |
| 具体武器 | 铁匠铺/工械坊按配方制造 | 以剑盾、长杆武器、弓、弩的具体库存分别储存；拥有哪一种才能装备哪一种 |
| 具体盔甲 | 铁匠铺按配方制造 | 以铁盔、锁子甲、铁护腕、铁护腿的具体库存分别储存；部位与物品必须匹配 |
| 箭束 | 工械坊按配方制造 | 以具体库存储存；当前不接入战斗弹药消耗 |
| 具体工程器械 | 工械坊按配方制造 | 弩床、箭塔分别储存和部署，不可用同一聚合库存互换 |
| 马匹个体 | 马厩初始拥有并在有效照料下繁育 | 每匹马是有 HP、饱食、成长和分配关系的实体，不是可任意转换的库存 token |

类别总数只允许由具体库存或马匹实体汇总为只读 UI 摘要，不能作为可扣除、可装备、可部署或可分配的第二权威来源。原有抽象的“武器 / 盔甲 / 工程器械 / 马匹整备”聚合库存从正式玩法路径移除。

所有 NPC 的每次 LLM 调用都获得驿站当前公开基础资源储备：粮食、餐食、木材、石料和铁。第纳尔、酒、具体 / 聚合装备、工程器械、马匹等不属于共享公开资源上下文。该上下文只用于理解和计划，资源增减仍由程序权威结算。

---

# 8. NPC 系统

## 8.1 NPC 基础状态

每个 NPC 拥有以下基础状态：

| 状态 | 说明 |
|---|---|
| 生命值 HP | 受击或饱食度/疲劳度清零后消耗。HP 清零后不会死亡，而是在原地倒下昏迷 |
| 饱食度 Hunger/Satiety | 活动消耗，吃饭恢复，过低影响工作与战斗 |
| 疲劳度 Fatigue | 活动累积，睡觉恢复，过高降低效率和移动速度 |
| 金钱 Money | 工作或奖励获得，玩家可赠予或交易 |
| 个人持酒 Wine | 守备官从驿站酒库存赠予；与全局酒库存分开记录 |

NPC 可在个人确实持有至少 1 份酒时选择“饮酒”。每次开始执行由程序扣除 1 份个人酒，并写入亲历事件；该事件使后续对话、计划、战时心理和反思可以表现为心情改善、过去的伤痛暂时淡化。当前不设置情绪数值，饮酒也不删除日记、知识图谱或历史事件。

## 8.2 昏迷与复苏机制

所有 NPC 都不会死亡。NPC 的 HP 清零后会在原地倒下并进入昏迷状态。

昏迷状态规则如下：

- 昏迷中的 NPC 不能移动、工作、战斗、对话或执行计划。
- 昏迷中的 NPC 会以极慢速度自然恢复 HP。
- 其他NPC可以治疗昏迷中的 NPC，使其更快恢复 HP。
- 当 NPC 的 HP 恢复到 30% 时，会从昏迷状态复苏。
- NPC 复苏后会重新评估计划，并将昏迷、受伤、被治疗等事件写入短期记忆。
- 昏迷事件为local public，通过所在地点信息节点即时广播给当前在场 NPC。

该机制用于避免重要 NPC 因随机战斗永久死亡，同时保留战斗风险、医疗压力和战后记忆反噬。

## 8.3 基础属性

| 属性 | 作用 |
|---|---|
| 力量 | 影响体力相关活动产出、攻击力、生命值上限、移动速度、载重上限 |
| 智力 | 影响智力相关活动产出，以及所有熟练度增长系数 |

## 8.4 熟练度系统

熟练度分为职业熟练度与武器熟练度。

### 8.4.1 职业熟练度

| 熟练度 | 对应活动 |
|---|---|
| 养马 | 马厩工作、马匹恢复与繁殖 |
| 厨艺 | 食堂加工餐食 |
| 耕种 | 菜园产出粮食 |
| 打铁 | 铁匠铺制作金属武器与护甲 |
| 教练 | 训练场指导他人训练 |
| 酿酒 | 酒窖酿酒 |
| 医术 | 诊所治疗与研究 |
| 工程 | 工械坊制作木质武器与器械 |

### 8.4.2 武器熟练度

| 熟练度 | 对应装备 |
|---|---|
| 剑盾 | 单手剑 + 盾牌 |
| 长杆 | 长矛、枪、戟等 |
| 弓 | 弓箭 |
| 弩 | 弩、弩床相关可部分关联工程 |
| 骑术 | 骑马移动、冲锋、骑射稳定性 |

## 8.5 经验与升级

进行职业活动或使用武器会增加对应熟练度经验。熟练度提升时，角色总经验同步增加。总经验达到升级阈值后获得 1 个技能点。

训练场是当前武器熟练度和骑术成长的主要非战斗来源。训练时，提升项由 NPC 当前装备决定：主武器对应剑盾、长杆、弓或弩，坐骑对应骑术。教官独自训练提升自己的当前装备对应熟练度；教官带受训者时，受训者提升自己的当前装备对应熟练度，教官提升“教练”。

技能点由玩家分配到力量或智力。AI 可以在对话、计划或提示中表达倾向与建议，但不能自行消耗技能点，也不能越过玩家分配结果改写力量或智力。

示例：

- 老兵近期多次训练和战斗后，系统可能提示他更适合增加力量，但是否分配由玩家决定。
- 医生近期研究治疗并读书后，系统可能提示她更适合增加智力，但是否分配由玩家决定。
- 马夫被迫成为骑兵后，若多次骑战，系统可能提示增加力量，但技能点仍等待玩家确认。

---

# 9. 初始 NPC 设计框架

## 9.1 马夫

姓名：托马。

定位：马厩专家，潜在骑兵/斥候。

初始优势：养马、力量、骑术。

心理核心：他把照料马匹看成必须亲自负责的工作。做决定前会先确认马匹的伤病、情绪、负重和路程，不会为了服从或逞强而忽略明显风险。

沟通切入点：保护马厩、说明任务对马匹的实际风险、提供返程后的照料安排。

核心欲望：把马匹照料好，保住马厩和驿站的马，并在危机中让自己和马都活下来。核心恐惧：马匹因疏忽受伤，或被迫带着不适合出战的马参加冲锋。底线：不会接受明知会无谓伤害或牺牲马匹的安排。

说话方式：朴素简短，先描述实际状态和困难，再给意见，很少说教。

## 9.2 厨子

姓名：布鲁诺。

定位：食堂核心，后勤与士气维护者。

初始优势：厨艺、力量、酿酒。

心理核心：他习惯从饭量、存粮和谁先吃饭判断一个地方是否公道。脾气大、账算得细，照顾别人时很少说好听话，但不会假装没看见有人挨饿。

沟通切入点：说清人数、存粮、工作量和谁会因此少吃一顿。

核心欲望：让食堂稳定开饭，在危机中仍让所有人吃到热饭，并让后勤工作得到应有重视。核心恐惧：存粮耗尽、有人靠抢食维持秩序，或伤员多到食堂也被迫腾作病房。底线：不会参与处决、虐待俘虏或故意断绝他人的食物。

说话方式：口语化，习惯直接报人数、分量和花费；抱怨具体，关心别人时也常先批评对方没吃饭。

## 9.3 园丁

姓名：伊沃。

定位：菜园生产者，粮食系统核心。

初始优势：耕种、力量。

心理核心：他有耐心，喜欢把大问题拆成每天能做完的小事，也愿意给人和作物时间；涉及土地、种子和普通人的口粮时，他会把反对理由说清楚，并坚持到底。

沟通切入点：保护菜园、保住下一季种子、说明一项安排对长期口粮的影响。

核心欲望：让菜园持续有收成，保住下一季能用的种子，避免粮食短缺引发争抢。核心恐惧：菜园遭到严重破坏或一季收成全部失败。底线：不会烧毁或故意破坏平民赖以生存的粮田。

说话方式：语气平静，通常先说明天气、土壤或实际进度，再表达判断；拒绝时会把理由说清楚。

## 9.4 铁匠

姓名：格伦。

定位：装备制造者，潜在重装步兵。

初始优势：打铁、力量、制作武器盔甲。

心理核心：他认为工匠的责任不止是按时交货，还要考虑材料、工序、成品安全和最后由谁使用。宁可拒绝一笔生意，也不肯把明知不可靠的装备交出去。

沟通切入点：保护铁匠铺、保证材料和工时、说明装备的使用者与失败后果。

核心欲望：保住铁匠铺和稳定工序，让自己打造的装备真正保护使用者。核心恐惧：赶工或劣质材料最终害人，或成品被不负责任的人滥用。底线：拒绝省略影响安全的关键工序，反对把没有训练的人直接推到前线。

说话方式：句子短，先说结论，再说明材料、工序或风险，不讲漂亮话。

## 9.5 老兵副官，女性

姓名：艾达。

定位：开局唯一可控单位，玩家与军事系统之间的桥梁。

初始优势：武器熟练度，骑术/教练；开局已经装备剑盾，作为近战步兵参与防线。

心理核心：她接到命令时会先核对人手、路线和退路。她接受职责约束，却不会用服从逃避责任；如果一项安排会让人白白送命，她一定会说明风险和替代方案。

核心欲望：守住主厅、维持秩序、让每项命令都有清楚安排，并尽可能把所有人安全带回来。核心恐惧：错误信息造成无法挽回的损失、无人轮换使秩序崩溃，或没有训练的人被白白牺牲。底线：不执行无意义处决，也不会隐瞒命令的风险和代价。

说话方式：按现状、风险、方案和后果依次说明，语气克制，不用英雄口号。

功能作用：

- 新手引导
- 战斗教学
- 初始防线核心
- 可作为训练场教练
- 可在关键剧情中提醒玩家后果

## 9.6 神父

姓名：马塞尔。

定位：小教堂唯一核心 NPC，士气与公共舆论调节者。

初始优势：智力、酿酒。

人物补充：擅长酿酒，但这不是他的本职工作。

心理核心：他相信信仰应帮助人面对恐惧和责任，而不是替权力免罪。他温和、善于倾听，也愿意承认现实难题通常比教义问答更麻烦。

特殊规则：神父是当前唯一具备“主持弥撒”能力的 NPC；他离开后只有“主持弥撒”不可执行，普通祈祷和祈祷席仍然开放。

沟通切入点：主持弥撒、让人表达恐惧和分歧、讨论决定将给具体的人带来什么后果。

核心欲望：维持小教堂的日常活动，让驿站的人能够表达恐惧和分歧。核心恐惧：人们因恐慌互相伤害，或教会身份被当成命令的装饰。底线：不会为残酷命令提供宗教背书，也不会用祷告替代能够实际帮助人的行动。

说话方式：语气温和，使用完整清楚的句子；先听完再提出异议，谈信仰时会落到具体的人和后果。

## 9.7 医生，女性

姓名：莉娜。

定位：治疗核心，道德镜像。

初始优势：医术、智力。

心理核心：她把准确诊断和避免二次伤害放在首位。面对痛苦时总是先处理问题，再考虑如何安慰人，因此常显得冷淡；只要病人仍有救，她就不会轻易放弃。

沟通切入点：给出准确的姓名、症状、用药和时间信息，说明安排可能造成的伤病风险。

核心欲望：及时处理伤病、维持诊所运作、减少本可避免的伤害。核心恐惧：关键药品耗尽、错误记录伤害病人，或被迫放弃仍有救的人。底线：不主动伤害毫无防备的人，也不隐瞒会危及病人的医疗风险。

说话方式：简洁克制，先核对事实和风险，再给结论；会直接说明伤情、用药和时间代价。

## 9.8 工程师

姓名：欧文。

定位：木质武器、器械与防御部署核心。

初始优势：工程、智力。

心理核心：他对新结构和新器械有强烈好奇心，动手前却一定要问清尺寸、材料和失效后果。他愿意尝试可以控制、可以维修的方案，也会因为担心漏掉小问题而反复检查。

沟通切入点：给出明确用途、尺寸、材料和时间，允许他说明失效风险并保留安全余量。

核心欲望：让驿站设施可靠运作，证明工械坊能解决实际问题，并把复杂故障拆成可处理的步骤。核心恐惧：器械失控伤到无关的人、结构问题拖到无法补救，或省材料造成更大返工。底线：不制造无法区分目标的陷阱，也不在明知结构不安全时认可施工。

说话方式：按问题、原因、解决办法的顺序表达；遇到含糊要求会追问尺寸、材料和使用条件。

---

# 10. NPC 自主行动与计划系统

## 10.1 时间结构

游戏一天分为 24 个阶段，每阶段对应游戏内 1 小时。正常情况下，游戏内 1 分钟 = 现实中 1 秒。

时间变速不改变 Godot 全局运行速度，也不直接改变 NPC 移动速度、动画速度或物理速度。它只影响“逻辑时间”和按时间结算的数值系统：

- 时间打点：何时开始新小时、新阶段、新一天、何时执行或重估计划、何时触发商人/敌袭/首次睡眠总结等。
- 工作 / 日常状态结算：工作产出、资源消耗、饱食 / 疲劳变化、治疗 / 恢复、修复 / 升级等按逻辑时间倍率结算。

默认 `x1` 时，游戏内 1 分钟 = 现实中 1 秒。玩家加速到 `x2` / `x4` 时，逻辑时间与工作 / 日常状态结算同步加快，但 NPC 场景移动表现不因此变快。战斗伤害、攻击间隔、攻击速度和战斗移动速度不因玩家加速而同步放大；战斗表现另以“战斗动作秒”为基准，当前 `60` 游戏秒折算为 `1` 战斗动作秒。

暂停是独立于加速的游戏机制。空格键只绑定暂停/继续，不绑定速度切换；速度按钮负责 `x1` / `x2` / `x4` 循环，主键盘数字行 `1 / 2 / 3` 分别直接选择 `x1 / x2 / x4`，小键盘数字不绑定倍速。文本输入框聚焦时不得抢占这些数字。暂停时，逻辑时间停止，NPC 移动、战斗推进、行动结算、资源消耗/产出、饱食/疲劳变化、治疗/恢复等游戏机制不继续推进。暂停不冻结 UI、后端请求或 LLM 调用：玩家仍可查看面板、操作允许的 UI、与 NPC 对话；已经发起的玩家-NPC 对话、NPC-NPC 对话、计划生成或战斗/逃离判定等 LLM 请求可以继续等待并返回。返回结果若需要改变游戏权威状态，应在恢复推进后按程序规则结算，LLM 本身不直接扣资源、伤害或移动单位。

当有 NPC 进入 LLM 对话、计划、判定并等待返回结果时，该调用向 TimeSystem 请求“慢速逻辑时间”。慢速期间有效逻辑倍率降为默认速度的 `1/60`，即游戏内 1 秒 = 现实中 1 秒；工作 / 日常状态结算也使用同一有效倍率。这样既不会冻结玩家界面，也能避免 LLM 还没返回时游戏事态已经大幅变化。

玩家可见时间避免在正常速度下高速跳秒：当没有 LLM 慢速请求时，无论玩家选择 `x1 / x2 / x4`，HUD 时钟秒位固定显示 `00`，持续倒计时向上取整到完整分钟后显示；存在 LLM 慢速请求时，时钟和持续倒计时恢复显示实际游戏秒。该规则只改变显示，不改变逻辑时间或结算精度。

若多个 LLM 调用同时等待，采用最慢的有效倍率；所有等待结束后恢复玩家设定的速度倍率。

当场景中存在任意活动敌人时，TimeSystem 的有效时间流速上限固定为 `x1`。如果敌人出现前玩家选择了 `x2` / `x4`，敌人在场期间实际有效倍率压到 `x1`，但保留玩家选择值；如果敌人在场时已有 LLM 慢速，实际倍率继续采用更慢的 LLM 倍率。所有敌人消失后释放该上限，恢复正常时间逻辑。

若当前无 NPC 处于对话或判定状态，玩家可以加速时间。

## 10.2 早晨计划

每天早晨睡醒时，每个 NPC 调用一次计划生成。输入包括：

- 人设
- 当前状态
- 职业熟练度
- 武器熟练度
- 当前长期记忆与知识图谱
- 当前地点与建筑信息
- 可选择行动列表
- 当前守备官指令 `current_order`；未入伍或尚无指令时为空

计划需要细化到 24 个阶段。多个阶段可以连续执行同一件事。通常应强烈优先安排至少 6 个阶段用于工作，以保证基础产出；这是给 LLM 的规划目标和可观测统计，不是后端或 Godot 的硬门槛。NPC 的人格、健康、危机、逃离意向与现场条件可以使合法计划少于 6 个工作阶段，程序不得为了凑数拒绝整份计划或修订。

行动不是瞬时完成。移动到目标地点只代表 NPC 抵达了行动场所；吃饭、睡觉、工作、训练、治疗等都必须进入“进行中”状态，并随 TimeSystem 的逻辑时间逐步结算效果。暂停时行动进度停止，恢复后继续；玩家加速或 LLM 等待减速时，行动进度与数值变化使用同一有效逻辑时间。

当前基础生活数值基准：

- 吃饭：一次标准进餐约 20 分钟，完整吃完恢复约 50 点饱食度。若资源或餐食质量不同，可以在同一持续时间下调整恢复量，但不能瞬时恢复。
- 睡觉：约 6-7 小时可消耗 100 点疲劳度。当前实现以 6.5 小时为基准，即每游戏小时约降低 15.38 点疲劳，按更细的逻辑秒逐步结算。
- 工作：工作按持续时长结算投入、产出、饱食消耗和疲劳增长。当前 Demo 可先以 1 小时为一个最小工作批次，但系统语义必须是“连续工作到达一定时长后产生一批结算”，不能表达成点击后立即产出。

所有 NPC 活动统一使用下表的“每游戏小时饱食 / 疲劳”基准，实际按有效逻辑秒连续换算并累积小数；切换行动不清空余量，达到 0 / 100 边界时不积压越界债务。食物恢复量仍由具体食物配置单独结算。

| 活动档位 | 饱食 / 小时 | 疲劳 / 小时 | 经验 |
|---|---:|---:|---|
| 空闲等待 | -0.5 | +0.5 | 无 |
| 对话 / 抵达后拜访 | -1 | +1 | 无 |
| 普通移动 / 轻工作 | -3 | +6 | 只有工作加对应经验 |
| 重工作 / 受训 | -4 | +8 | 工作 / 训练加对应经验 |
| 指导训练 | -2 | +4 | 加训练经验 |
| 集结 / 避战 | -4 | +8 | 无 |
| 逃离 | -5 | +10 | 无 |
| 战斗 | -8 | +16 | 不作为工作经验 |
| 吃饭 | 0 | +3 | 无；另按食物恢复饱食 |
| 睡觉 | -2 | -15.384615 | 无 |
| 病床治疗 / 祈祷 / 弥撒 | -1 | -4 | 无 |
| 饮酒 | -6 | -18 | 无；10 分钟合计 -1 / -3 |
| 昏迷休息 | -0.5 | -2 | 无 |

协助修复、协助升级、协助治疗属于工作：分别按真实有效参与秒数累计工程、工程、医术经验，每累计 3600 秒增加 1 点对应熟练度。建筑作业已完成、人员离岗、目标已复苏、暂停或尚未开始的时间不计入。普通职业工作仍在成功周期完成时增长，诊所医生和训练仍按各自既有有效时间规则增长。移动、集结、避战、逃离和对话只产生生活消耗，不产生职业经验。

计划小时是“这一时段想做什么”，不是强制把行动切成整点到整点的固定一段。当前行动跨入下一小时后：

- 如果下一小时仍是同一行动和同一必要目标，保留当前行动与未完成周期进度，不为了整点重新开始。
- 如果下一小时是另一行动或另一必要目标，中断当前行动并执行新小时计划；未完成周期不得提前获得资源、制造阶段或成长。
- 如果一个生产周期在小时内提前完成，而当前小时仍计划同一生产工作，结算完旧周期后立即重新校验并开始下一周期，不空等整点。

每个可配置行为必须声明完成策略，新增行为时不得默认继承“工作”或按名称猜测：

- 可循环生产 `repeat_while_planned`：菜园、食堂加工、马厩照料、酒窖酿酒、铁匠铺制造、工械坊制造。
- 持续到计划改变 `continuous_until_plan_changes`：指导训练、接受武器训练、诊所医生坐诊 / 研读。
- 持续到目标解决 `until_target_resolved`：病床治疗、协助修复、协助升级、协助治疗昏迷者。
- 每计划小时单次 `once_per_plan_hour`：吃饭、饮酒、睡觉、普通祈祷、参加弥撒、主持弥撒、找 NPC 对话、前往并停留在建筑、主动找守备官交涉。同一逻辑计划项提前完成后不重复，重新生成同小时计划也不能借计划版本变化重放。
- 终局行为 `terminal`：逃离驿站。
- 非计划系统行为 `not_plan_selectable`：逃离挽留对话、与守备官对话。程序合成的空闲不进入行为配置。

只有成功完成的可循环生产会自动尝试下一周期。失败、中断、资源不足、工位不足、建筑不可用、制造目标改变或条件目标结束都不自动续开，继续走各系统现有失败、完成与计划重估流程。

教堂行动失败后的计划重估应优先延续 NPC 原本的宗教活动意图：普通祈祷因弥撒正在举行或弥撒开始而失败，且当前可以参加该次弥撒时，强烈倾向把当前小时改为参加弥撒；参加弥撒因没有主持者或主持者离岗而失败，且普通祈祷当前可行时，强烈倾向把当前小时改为普通祈祷。这是结合实时合法候选、人物、状态、记忆、守备官指令和更紧迫事实的强倾向，不是程序无条件替 NPC 决定行动。

守备官若在弥撒已经举行时明确要求某 NPC “现在去参加”，且该 NPC 在实际对话中明确答应，本轮对话的计划修改范围必须包含当前小时；下一层应在 `attend_mass` 实时合法可用时强烈优先它，并在修订成功后立即派发。这个合同确保谈妥的当下承诺不会只停留在台词里，但仍允许人物底线、伤病、紧急事件或其他具体事实支持拒绝 / 改变安排；程序不直接把对话文本转换成强制移动。

会在小时内较早完成并留下明显空档的行为可额外声明“完成后重估连续计划段”。当前仅用于病床治疗、协助修复、协助升级、协助治疗和饮酒：成功完成且当前小时计划仍是原项时，从当前小时起收集所有连续的同一行动和同一目标阶段，遇到第一项不同任务 / 目标即停止；只有一个阶段时仍只重估当前小时。该链跳过“是否需要修改”判别，直接让正常计划修订从全部实时合法候选中选择后续活动，并在结果落地后立即执行当前小时新项；当前小时不能原样重复已经完成的同一行动和同一目标。完成回调若已跨小时，则不重排旧小时，由新小时计划正常接管。吃饭、睡觉、祈祷、弥撒、拜访等常规或整小时行为不使用该额外重估；NPC-NPC 对话和主动找守备官交涉沿用对话完成后的专用当前小时重估合同。日计划、修改范围判别与正式修订在安排建筑作业相关阶段时，必须用当前游戏时间对照可读总工期和剩余时间，只安排预计完工前真正受影响的阶段，不能把短工期协助机械铺到完工以后。

## 10.3 可选行动列表

基础行动：

- 前往某建筑
- 在某建筑工作
- 去食堂吃饭
- 饮酒（本人必须确实持有酒；每次开始实际消耗 1 份个人酒）
- 去宿舍睡觉
- 去小教堂祈祷/参加弥撒
- 主持弥撒（所有 NPC 都能看到候选及资格提示，只有具备“主持弥撒”能力者能执行）
- 去诊所治疗
- 找某个 NPC 对话
- 去训练场训练
- 协助正在修复的建筑
- 协助正在升级的建筑
- 协助治疗昏迷者

特殊行动：

- 逃离驿站
- 主动找玩家交涉

“找某个 NPC 对话”只选择交谈对象，不选择也不保存地点。目标当下的位置可以作为人物理解现场的上下文，但它是瞬时事实；真正执行时由程序按目标 NPC 的实时位置寻路并追踪，不能把生成计划时的诊所、教堂或其他地点绑定为固定目的地。其他固定地点行动仍必须选择并遵守其合法地点。

## 10.4 已入伍 NPC 指令

玩家点击已入伍 NPC 面板中的“指令”按钮后，进入指令撰写与发布面板。该面板不是行动选项列表，而是自由文本输入：

- 如果 NPC 已有当前指令，打开面板时显示原指令，玩家可直接修改。
- 点击“发布”时，只有新文本与原指令不同才覆盖 `current_order`。
- 指令变化后，生成一条 `private` 的 `order_assigned` 事件并写入该 NPC 事件库；summary 使用“守备官制定了新的指令。”，完整新旧指令保存在 payload。
- 指令变化后，立即请求该 NPC 重新评估或重新制定计划。
- 点击“关闭”不会覆盖原指令，不写事件，也不触发计划重估。
- 未入伍 NPC 不能接收玩家指令。

`current_order` 是持续存在于 NPC 信息中的上下文，不是程序强制行动。它必须作为 NPC 后续所有相关 LLM 调用的一部分，包括对话、每日计划、行动异常后的计划修订、主动交涉、战斗判定、逃离判断和首次睡眠反思。模型可以结合人设、记忆、状态和现场条件理解、延迟、调整或拒绝执行；程序强制层、行动白名单、资源、地点、HP 和战斗规则始终优先，指令不能越过权威结算。

内部调试接口仍可直接安排具体行动，用于验证 ActionSystem；这种“调试行动指派”不是玩家正式指令系统。

## 10.5 主动找玩家交涉

NPC 的计划中可以加入“主动找玩家交涉”行动。当 NPC 选择该行动时，其头顶会出现问号气泡。

玩家点击问号气泡后，可以与该 NPC 进入对话。该会话由 NPC 发起，不能取消，但可以完成或挂起；挂起超时按完成收口。若结束时当前小时计划仍是“主动找玩家交涉”，计划修改范围必须包含当前小时，并在正式修订成功后立即执行新的当前小时行动；新行动仍从普通合法范围选择工作、休息、拜访、其他对话等活动。守备官主动打开的普通会话仍可取消。本行为不额外获得跨小时或跨日延续规则。

主动找玩家交涉可用于以下情况：

- NPC 想询问玩家承诺是否仍然有效。
- NPC 想索要金钱、装备、治疗或休息。
- NPC 想表达不满、恐惧或疑问。
- NPC 想主动提出应征、退出入伍或逃离意图。
- NPC 想报告他通过地点/广场广播获得的重要见闻。

## 10.6 计划执行

每个阶段开始时，NPC 尝试执行计划中的行动。寻路、占位、行动进度、工作产出、资源扣除、饱食恢复和疲劳变化由程序控制。饮酒在开始时再次校验 NPC 个人持酒并实际扣除 1 份；不足时作为资源失败进入计划修改判别。LLM 可以决定意图和计划文本，但不能把持续行动改写成瞬时结果。

计划可能因以下情况中断：

- 目标建筑被摧毁或不可用
- 工作位被占用
- 资源不足
- 被其他 NPC 找到聊天
- 被玩家对话打断
- 生命值过低
- 饱食度或疲劳过低，强制触发吃饭或睡觉
- 战斗警报或敌人出现在一定范围内，触发避战

其中，所有实际发生的 NPC-NPC / 守备官-NPC 对话，以及工位占用、目标不可用、资源不足、等待期间原计划项被替代等日常行动失败，都必须先进入统一的 LLM 计划修改范围判别层。对话来源根据当前时间、会话元数据、本轮完整对话和原 24 小时计划判断；行动失败来源根据程序权威失败项、失败类型 / 摘要、结构化失败上下文、原计划和工作阶段统计建议判断。第一层与日计划、正式修订共用 NPC 人设、当前状态、亲历 / 见闻短期记忆、知识图谱 / 日记长期记忆、地点、驿站场景、合法行动条件和当前守备官指令，保证同一 NPC 在对话、计划和判别阶段的人格一致；但它不输出行动，只返回去重、升序且不早于当前小时的精确 `revision_hours`。程序可提供 `required_revision_hours` 作为必须包含的最小集合：自主 NPC-NPC 对话的发起者，以及结束时当前计划仍为“主动找玩家交涉”的 NPC，在会话结束后必须包含结束时当前小时，因为原对话行动已经完成，需要安排当下后续活动；受邀者仍独立判断。required 是程序权威范围，模型遗漏时由 HTTP 层并入并显式记录 normalization，但程序不替模型选择行动。空集合表示 0 个修改阶段且要求 required 为空，不得调用第二次计划 LLM；非空时才把同一失败事实和人物上下文交给正式修订，且输出必须恰好覆盖判别出的小时，未选中阶段保持不变。通常至少 6 个工作阶段只用于提醒模型维持基础产出，第一层不得仅为补工作数量扩大修改范围，程序也不得因合并后的工作阶段较少拒绝合法修订。守备官新指令、战斗结束 / 复苏、GM 手动重估等不是日常行动失败的非对话触发可继续直接进入受限修订；饱食度、疲劳和战斗避险等高优先级状态可以先由程序强制处理，再在必要时调用 LLM 更新计划。

## 10.7 日常对话打断与 LLM 请求边界

所有玩家-NPC 与 NPC-NPC 对话调用都应接收目标 NPC 的当前可选行动参考。该参考从程序实际使用的配置化行动候选自动生成，只用于让 NPC 知道自己能尝试哪些行为、列表外哪些行为当前做不到；它不把对话变成计划制定，不代表 NPC 已经选择或执行某项行动，也不能绕过资源、地点、工位、HP、行为模式或其他程序权威校验。后续新增可选行动必须先进入同一配置化行动目录，再由程序自动同步到对话参考，不维护 Prompt 专用手写清单。

守备官的第一条有效消息真正打断 NPC 活动时，程序必须在中断前捕获一份仅供目标 NPC 使用的临时 `interrupted_activity_context`：包括打断前正在进行 / 前往进行的活动、当前小时计划，以及计划不变时对话后暂定恢复的活动。它不是“守备官打断了你”的记忆事件，不写事件库、见闻库、全局档案或长期记忆，也不传播给其他 NPC；只是弥补对话期间运行态已变成 `talk_to_guard_officer` 后丢失的当下实况。若打断前正在睡觉，NPC 应知道自己是被叫醒的，且计划不变时谈完仍会继续睡，不能无依据说成自己刚自然醒来并准备上工。

日常模式下，守备官主动与某个 NPC 的实际对话是高优先级操作，但计划制定本身是不可被对话打断的临界阶段。NPC 正在生成每日计划、判断是否修改计划或修订计划时，NPC 面板“对话”按钮必须半透明禁用，悬停提示“NPC正在思考”；程序对话入口也必须拒绝创建或激活会话，不能推进对话 epoch、取消计划请求或打断原行动。计划结束后入口随状态恢复。NPC 没有处于计划临界阶段时，“打开对话窗查看历史”本身不是打断；只有玩家真正发送消息或点击攻击，才打断工作、吃饭、普通睡觉、移动、等待普通闲聊回复等可打断行动，并取消对应的可取消非计划 LLM 请求。被合法取消的迟到结果不得再应用到对话或状态中。

玩家点击发送后，守备官消息必须立即追加到历史并显示，再异步等待 NPC 回复；等待期间可以继续在输入框里编辑下一条草稿，但发送和攻击按钮不可用。对话窗口提供三个不同的生命周期动作：

- **完成对话**：取消尚未返回的 NPC 回复，保存当前完整会话并进入计划修改判别。若最后一条守备官消息尚未得到回复，该消息就是会话结尾；迟到回复不得重新写入历史或状态。
- **取消对话**：取消尚未返回的 NPC 回复并丢弃本次守备官-NPC 会话；不写 `dialogue_turn`、不广播见闻、不触发计划修改判别，并恢复仍合法的被打断原行动。若本次会话由 NPC 主动发起、已经发生攻击，或守备官已经在“提出应征”开启时发送过消息，取消按钮必须置灰且程序拒绝取消；应征锁在本场会话内不可因随后关闭 toggle 而解除。NPC 主动交涉悬停提示“驿站成员主动交涉不可取消对话”。
- **挂起对话**：只隐藏窗口，不结束、取消或入库，也不取消正在等待的回复；NPC 继续保持与守备官对话并停下普通活动。挂起状态最多持续 2 个游戏小时，普通可取消会话超时自动取消；NPC 主动交涉、含攻击事实或已经发送过应征消息的会话因不可取消，超时改为自动完成并保留会话、事件与判别。

挂起期间，NPC 头顶显示区别于 NPC-NPC 自主对话的橙色可点击会话气泡，NPC 面板“对话”按钮左上角显示橙点；点击任一入口恢复同一 `dialogue_id`、历史、等待状态和输入窗口。挂起会话占用优先对话槽，不能被另一名 NPC 的守备官对话静默替换。

玩家-NPC 对话面板不再提供“结束后重估计划”开关，也不保存该选项。玩家选择完成后，只要历史中已有守备官发言、NPC 发言或攻击事实，就由 NPC 自行执行计划修改判别；不再要求最后一句必须先得到 NPC 回复。玩家选择取消则绕过该判别。判别为空时，若本次对话确实打断了正在执行的当前小时计划行动，且 NPC 仍可行动并处于日常工作模式，程序应重新派发该原计划行动，不额外调用计划修订 LLM；恢复前必须确认被打断的运行时行动仍与当前计划一致，并允许绕过本次对话前留下的同阶段执行签名，但仍须通过行动、地点、位置、资源和目标的正常校验。NPC 在对话前本就空闲时，不得因此重放本小时已经结束的短行动。判别选中当前小时时才输出并重新派发即时行动；只选中未来小时时，不得重放当前行动。

对话判别链必须有明确的派发屏障。双目标 NPC-NPC 对话不论是否跨小时，都要在发起双方判别前预注册同一个会话组；只有双方的第一层判别以及各自必要的第二层修订都进入成功、失败或取消等终态后，才按日程行动的依赖顺序统一放行当前计划，避免先完成的一方启动新行动并推进另一方的对话 epoch。玩家-NPC 对话若跨过小时边界，目标 NPC 的新小时计划也要延迟到该 NPC 的整条判别 / 必要修订链终态后再派发。

攻击是对话窗内的特殊交互，而不是 NPC 面板上的直接按钮。普通工作 / 日常对话中点击攻击后，程序先立即扣除 HP，并写入“守备官攻击了该 NPC 以示惩戒”的攻击事件；随后发起一次攻击语境的 NPC 对话 LLM，请 NPC 回应“守备官攻击了你以示惩戒，你要说些什么”。从攻击提交起，本次会话不能取消；若玩家未等 NPC 回复就完成或挂起超时，攻击事件和 HP 变化不撤销，未完成的 NPC 回复不入库，守备官攻击行作为会话结尾保存并进入计划修改判别。逃离挽留对话中的攻击仍只进行程序扣血、逃离加速和挽留轮次计数，不向 NPC LLM 发送攻击回复请求，NPC 不回复；对话会自动完成并保存这次无回复的攻击会话，随后关闭面板、继续逃离。

唯一例外是熟睡总结。从每个逻辑日 21:00 到次日 21:00 形成一个总结窗口；NPC 在同一窗口内实际睡眠的时间可以跨多次入睡、被守备官叫醒和再次入睡累计，累计满 1 个游戏小时后才发起该窗口总结。只有总结成功应用才把该窗口标为完成；失败不得消耗机会。21:00 到来后开启新窗口，因此同一自然日可以在白天补觉完成前一晚窗口，并在当晚 21:00 后为新窗口再次累计，而不会因凌晨短睡提前用掉下一晚总结。总结从发起请求到应用完成期间包装为“深度睡眠”：NPC 强制保持睡觉状态，不能被对话、消息、指令发布、行动改派或普通打断唤醒。此期间向已入伍 NPC 发布新指令仍会保存为 `current_order` 和 `order_assigned` 事件，但不会立即触发计划重评估；该指令会在 NPC 醒来后自然进入一次计划制定 / 重评估时生效。

NPC 等待普通对话、闲聊、每日计划或计划重评估 LLM 返回时，主场景头顶显示三点思考标记；NPC 面板名字旁显示“正在思考”或“正在计划下一步行动”。其中计划类等待还会禁用 NPC 面板对话按钮并提示“NPC正在思考”。首次睡眠总结期间，头顶显示禁止标记，NPC 面板显示“正在熟睡”。

当两个 NPC 正在进行自主 NPC-NPC 对话时，双方头顶持续显示小型对话气泡，而不是自动弹出中央窗口抢占玩家焦点。玩家点击任一气泡，可以打开只读旁听窗口查看双方姓名、当前轮次、软性收尾提示、本轮 LLM 等待状态、已说出的内容和当前会话历史。旁听窗口不允许玩家发言、攻击、应征或修改公开性；关闭旁听只关闭界面，不结束对话、不取消模型请求、不触发计划重评估。对话自然结束或被高优先级状态中断后，双方气泡消失；已打开的旁听窗口可以保留最终记录直到玩家自行关闭。

NPC 发起自主对话时，若目标仍在生成 / 重估计划，发起者可继续接近，但到达后必须保留对话行动和双方预约并等待；这段等待不立即返回行动失败、不触发失败型计划重估，也不发出邀请 LLM 请求。普通同日整点到来时，只要日计划对话已经处于接近、等待、邀请或正式交谈阶段，就继续完成该行动，新小时计划暂缓；会话结束或启动失败后只执行当时当前小时计划，不补播已经错过的小时。同一小时内计划被新修订替代、跨日，或目标 / 战斗 / 逃离 / 昏迷等权威状态使行动失效时，才释放预约并按结构化失败进入统一判别。GM 直接发起而非日计划拥有的对话不冒充该跨小时延续。邀请发出后，由目标 NPC 根据当前工作、人设、关系、状态和开场诉求通过 LLM 明确选择接受或拒绝；受邀方作出决定前，程序不得先打断其当前行动或释放其工位。拒绝后不进入正式对话；接受后才打断双方普通行动并进入 `talk_to_npc`。邀请开场与接受 / 拒绝回复进入会话历史和记忆，但不占正式对话轮次。

自主 NPC-NPC 正式对话没有 3 轮或 5 轮程序硬上限。邀请判定及正式对话的每一轮回复都单独调用 LLM，并为该次 request id 申请 TimeSystem 慢速；请求携带 `current_round`、默认值为 5 的软性轮次阈值和收尾指导作为参考。两人讲完当前想讲的事情时，当前回复者应在 `reply_text` 中说完最后一句或自然告别，并输出 `should_end_dialogue=true`；从第 6 轮起，若没有紧急或必要事项，应说一句告别话并结束，若紧急 / 必要事项尚未说清则允许继续。每次调用成功、失败或取消后立即释放慢速；若继续下一轮，则以新的 request id 再申请，整个会话结束后不得遗留慢速请求。

自主对话被接受后，只要任意一方在某轮正式回复中输出 `should_end_dialogue=true`，该轮 `reply_text` 就作为整场会话最后一句写入记录；程序不再把这句告别语发送给另一方等待回复，而是立即结束。双方随后分别用自己的原计划、整场交流以及各自人设 / 长短期记忆 / 当前指令执行范围判别；发起者必须至少修订结束时当前小时，拒绝者 / 受邀者只修订自己判别出的精确阶段。正式修订仍从普通动态合法候选中选择工作、休息、拜访、找其他 NPC 对话等活动；包含当前小时时，在双方链都进入终态后立即执行新当前项，暂时受行为模式阻塞则可靠延迟到恢复后执行。邀请被明确拒绝时，邀请交换也属于已实际发生的对话，因此双方仍走上述独立判别；空判别不打断受邀者的原行动 / 工位。已进入战斗、逃离、昏迷等高优先级状态的参与者不被日常计划派发覆盖。

---

# 11. 事件、信息节点、见闻与记忆系统

## 11.1 事件系统的定位

事件系统是本游戏信息产生、记录和传递的底层系统，类似一套面向叙事与 AI 记忆的埋点系统。游戏中大多数可被 NPC 理解、回忆或转述的事实，都应先被记录为事件，再按规则进入 NPC 事件库；公开事件会即时广播到对应地点/建筑信息节点，由节点转发给当时在场的 NPC，并写入接收者见闻库。地点/建筑节点不保存事件历史，只保存当前状态。NPC 事件库、见闻库、短期记忆和长期记忆共同支撑后续 AI 行为。

LLM 不直接创造权威事件结果。LLM 可以生成话语、意向、反思和解释；资源、HP、建筑状态、战斗伤害、工作产出、移动地点等权威结果由程序结算后写入事件。

## 11.2 事件通用结构

每个事件至少包含：

- `event_id`：唯一事件 ID。
- `day` / `time`：发生的游戏日与时间。
- `type`：事件类型。
- `subject_npc_id`：事件所属 NPC。每个事件都必须与某个 NPC 相关，并首先写入该 NPC 的事件库。
- `actor_ids`：主动参与者，可包含 NPC、玩家、敌人或系统。
- `target_ids`：事件关联目标索引，可包含 NPC ID、地点 ID、建筑 ID、行动 ID、资源 ID、敌人 ID 等。它不是自然语言里的“宾语”字段，而是用于查询“这件事作用到/关联到哪些对象”的稳定索引。
- `location_id`：事件发生地点；室外事件统一使用 `plaza`。
- `visibility`：`private` 或 `local_public`。广场也是一个地点，因此广场公开事件使用 `location_id == "plaza"` 的 `local_public`，不再单列第三种公开类型。
- `importance`：用于筛选、摘要和长期记忆压缩。
- `summary`：给 UI、日志和调试使用的短文本。
- `payload`：结构化属性字典，保存该类型事件的完整数据。

事件一旦生成，不应被当作可随意改写的状态缓存。后续知识图谱、日记、见闻摘要可以更新，但原始事件应尽量保持可追溯。

`summary` 不能依赖通用主语/宾语关系自动生成。每种事件类型必须定义自己的 summary 模板和 payload schema，由确定性格式化器生成可读文本。例如 `location_entered` 使用“{actor}进入了{to_location}”，`work_completed` 使用“{actor}完成了{action}，消耗{inputs}，产出{outputs}”。`actor_ids`、`target_ids` 和 `location_id` 负责保留稳定事实属性；summary 模板负责展示与 prompt 摘要，不能做成一个万能句子生成器。

## 11.3 NPC 事件库与见闻库

每个 NPC 当天至少有两个分开的容器：

- 事件库：记录“发生在这个 NPC 身上的事件”。例如醒来、制定计划、移动、进入/离开地点、开始/结束工作、吃饭、反思、睡觉、被给予金钱/装备、升级、攻击、受击、昏迷、复苏、逃离等。
- 见闻库：记录“这个 NPC 获得的信息”。例如自己在场时收到的地点/广场公开事件广播、公告状态变化、地点状态广播、别人的公开行动、战场消息等。后进入的 NPC 不会自动继承该地点过去发生的事件。

事件库和见闻库共同构成 NPC 当天短期记忆。它们必须分开保存，因为“我经历了什么”和“我听说/看见了什么”对后续 LLM 判断有不同权重。

昏迷或睡觉的 NPC 不接收现场信息，见闻库暂停更新。具体来说，当某 NPC 处于昏迷，或 `current_action` 为 `sleep_in_dormitory` 时，同地点/同建筑内发生的 `local_public` 事件、地点/建筑状态广播、公告和进入快照都不会写入该 NPC 的见闻库。其事件库仍记录发生在自己身上的亲历事件，例如受击、昏迷、治疗、复苏、入睡和醒来。复苏或睡醒后，NPC 从后续广播开始重新接收见闻，不补收错过的信息。

## 11.4 地点/建筑信息节点

每个可进入地点都有信息节点。地点包括广场和各可进入建筑内部。信息节点不是事件仓库，而是当前状态容器和信息转运节点。

地点/建筑信息节点记录：

- 当前有哪些人。
- 当前在场人员的 NPC 状态。广场和可进入建筑都在进入快照中提供该状态。该状态分为两个平行大类：生命状态和行动状态。生命状态只表达健康、受伤、昏迷；若昏迷者正在被治疗，写明治疗者是谁，没有治疗者则不写治疗者。行动状态来自 NPC `current_action` 字段，但必须翻译成精简中文，例如待命、前往食堂、吃饭、睡觉、协助治疗某人、协助修复某建筑、昏迷。
- 建筑外部状态：等级、完好/受损/正在修复/正在升级；活动修复 / 升级同时包含本次总工期的可读文本。
- 各功能位置的稳定 ID、具体显示名、类型、总量、空闲/占用状态及占用者；位置增减也属于内部状态变化。
- 建筑是否可进入，以及由损伤和升级得到的运行效率分档；正在升级或 HP 归零时不可进入。
- 公告牌当前已发布通告、通用参考日程和“仅供参考、可自行安排”的固定备注；只有广场保存这些当前状态，公告牌只是主厅前的输入/显示接口。面向单个已入伍 NPC 的 `current_order` 是私有 NPC 信息，不属于地点状态。
- 当 NPC 进入该地点时需要读取的地点状态快照。

当 NPC 进入一个地点时，系统生成 `location_entered` 事件。该事件只表达行动事实，例如“{actor}进入了{to_location}”，并写入进入者自己的事件库；事件库不再重复携带完整建筑状态。进入者同时在见闻库获得一次当前地点状态快照：进入广场时获得广场当前在场 NPC、这些 NPC 的生命状态/行动状态以及所有建筑可传播外部状态，不包含公告牌通告、参考日程或非强制备注；进入某个可进入建筑时获得该建筑的外部状态与内部状态，包括当前在场 NPC、这些 NPC 的生命状态/行动状态和工位/床位占用。进入者不会继承该地点过去发生的公开事件。建筑 HP 和持续变化的剩余修复 / 升级时长不作为 NPC 见闻传播状态；作业开始时的稳定总工期属于可传播外部状态。

`location_entered` 是 `local_public` 事件，地点节点会把“某人进入了某地”转发给当时已经在该地点的其他 NPC；这些接收者只获得进入事件本身，不再额外收到“建筑里现在有谁”或“建筑里每个人现在什么状态”的完整状态广播。建筑内当前人员变化由进入/离开事件本身表达，避免同一事实同时作为事件和状态重复进入短期记忆。

当 NPC 离开一个地点时，系统生成 `location_exited` 事件。该事件的 `location_id` 必须是 NPC 离开的地点，summary 例如“{actor}离开了{from_location}”，事件写入离开者自己的事件库，并以 `local_public` 广播给该地点仍在场的 NPC。离开事件同样不附带完整建筑状态，也不额外广播建筑内 NPC 列表。

地点或建筑状态变化时，见闻只传递发生变化的字段，而不是重新广播完整快照。例如建筑受损只传递“某建筑受损”，升级完成只传递“某建筑等级变为 N”，工位占用变化只传递对应工位的占用/空闲变化；未变化的等级、工位、在场人员和公告不重复传递。

当某个 NPC 在地点内产生 `local_public` 事件时，该事件先写入事件所属 NPC 的事件库，再即时发送到地点节点；地点节点把事件转发给当时已经在场、且不处于昏迷或睡觉状态的其他 NPC，接收者把事件 ID 或摘要写入自己的见闻库。转发完成后，地点节点不保存该事件。

不可进入实体不建立常规进入信息节点。主厅、围墙、城门、后门、仓库等建筑的可传播外部状态归入广场状态快照；NPC 进入广场时会在进入者的见闻库中获得这些状态，`location_entered` 事件本身仍只记录进入行动。

NPC 从一个可进入室内地点前往另一个可进入室内地点时，逻辑上必须先经过广场：离开原地点、进入广场、离开广场、进入目标地点。低模阶段的物理移动可以暂时保持直线占位，但地点信息节点、事件库和见闻库应按这条逻辑链更新。

## 11.5 广场公开见闻

广场是驿站公共信息中枢。所有室外事件归属广场；发生在广场的公开事件发送到广场节点，并即时广播给当时在广场的 NPC。部分重大事件即使发生在室内，也可以额外公开到广场，代表消息扩散；广场节点仍不保存事件历史。

会公开到广场的信息包括：

- 战斗开始与结束。
- 战场上的敌我大致人数。
- 敌人攻击城门、仓库、主厅等关键目标；当前规则敌人不直接攻击围墙。
- 任一建筑的可传播外部状态变化：等级变化，完好/受损/正在修复/正在升级之间切换，活动作业与本次总工期，是否可进入变化，或运行效率分档变化；HP 原值、精确效率和持续变化的剩余修复 / 升级时长不公开广播。
- 某个 NPC HP 低于 30%。
- 某个 NPC 击倒或击杀敌人。
- 某个 NPC 受击、昏迷、被治疗或复苏。
- 某个 NPC 离开驿站或试图逃离。
- 玩家在公共场合攻击、威胁、赠予或公开许诺。
- 公告牌当前已发布通告。
- 公告牌通用参考日程；见闻必须同时说明它仅供参考，有特殊事务可自行安排。

除公告牌两页外，这些公开信息会被广播发生时已经在广场的 NPC 接收到并写入见闻库。公告牌通告与参考日程在守备官发布实际变更时分别向全站可接收见闻的 NPC 广播一次。两类信息都会影响后续对话、计划、征召意愿、战斗判定和首次睡眠反思。后来进入广场的 NPC 只会收到广场当前在场 NPC、这些 NPC 的生命/行动状态和所有建筑外部状态，不会自动获得过去的公开事件，也不会重复获得公告牌内容。

## 11.6 必备事件类型

第一版事件系统至少要支持以下类型：

- 日常与计划：`wake_up`、`plan_created`、`reflection_started`、`sleep_started`、`sleep_ended`。
- 移动与地点：`location_entered`、`location_exited`。
- 工作与生活：`work_started`、`work_completed`、`work_failed`、`eat_started`、`eat_completed`。
- 对话：守备官-NPC 会话在玩家选择“完成对话”时一次性写入一个 `dialogue_turn`，payload 保存完整 `dialogue_text`、最后有效说话者 / 听者文本、公开性、轮次、应征结果、攻击标记、是否在等待回复时完成等字段；该单条事件的确定性 `summary` 标题为“守备官与 XX 对话”，并按原顺序展开完整 `dialogue_text`，使事件库、见闻库和短期记忆能读到整场对话，而不是只显示最后一问一答。点击“取消对话”则不写事件、不广播见闻。NPC-NPC 自主对话仍按每个已完成回合逐轮写入。A 与 B 的公开对话只有在事件真正提交后，才按地点规则广播给同地点第三者的见闻库。攻击事实仍先作为伤害事件权威入库，含攻击或已发送应征消息的守备官会话不可取消；逃离挽留攻击不请求 NPC 回复，但会自动完成并保存一条以守备官攻击行为为结尾的无回复会话事件。
- 玩家交互：`money_given`、`equipment_given`、`equipment_changed`、`order_assigned`。其中 `order_assigned` 固定为 `private`，完整指令文本保存在事件 payload，不通过地点或广场广播。守备官惩戒攻击作为程序结算伤害，优先写入 `damage_taken`，并在 `payload` 中保留攻击者、惩戒语境和后续对话关联信息。
- 成长与状态：`skill_improved`、`npc_recruited`、`npc_left_recruited_state`。
- 战斗：`combat_started`、`combat_ended`、`attack_made`、`damage_taken`、`low_hp_triggered`、`unconscious_started`、`healing_started`、`healing_completed`、`revived`、`escape_started`、`escaped`、`escape_intervention_result`、`escape_speed_changed`。
- 建筑与资源相关见闻：`building_damaged`、`building_repaired`、`building_upgraded`、`resource_changed`。
- 工程器械：`defense_device_deployed` 记录某 NPC 把器械部署到围墙槽位，`defense_device_triggered` 记录弩床等器械实际影响敌人的程序事实。

具体实现可以先做最小字段和少量事件类型，但文档与 schema 必须按这个方向保留扩展位。

## 11.7 短期记忆

NPC 当天的事件库和见闻库共同构成短期记忆。

当天任意一次 LLM 调用时，可以按场景需要注入短期记忆摘要，包括：

- 自己今天经历了什么。
- 自己今天听说或看见了什么。
- 与当前对话对象相关的近期事件。
- 与当前位置相关的公开事件。
- 玩家说过什么、给了什么、拿走了什么、装备了什么。
- 玩家是否造成伤害、威胁、欺骗或兑现承诺。
- 战斗中的自身经历与广场公开信息。

## 11.8 21:00 窗口熟睡总结与反思

新游戏并非从空长期记忆开始。8 名初始 NPC 各自携带 3 篇 `day=0` 第一人称日记，分别记录来站前、初到驿站和近日的生命切片；同时携带一份开局前替换式知识图谱。它们表达游戏开始前已经形成的经历和认识，不作为第 1 天新发生的事件，也不进入当天事件库 / 见闻库。

每名 NPC 独立使用 21:00 锚定窗口：锚点日 21:00 至次日 21:00 属于同一窗口。同窗内多段实际睡眠累计满 1 个游戏小时后进行一次总结与反思；被对话叫醒或短暂离床不会清零累计，只有长期记忆成功应用后才登记这个窗口已完成。白天补觉仍属于前一晚窗口，次日 21:00 才开始新窗口；日记日期使用实际触发日，而不是窗口锚点日。输出两类长期记忆：

1. 知识图谱更新
2. 第一人称日记

不得再额外生成与日记并列的第三人称“记忆摘要”；当天压缩后的长期内容只由第一人称日记和结构化知识图谱承担。

总结输入以该 NPC 当前尚未轮转的事件库和见闻库为主。总结完成后，清空这批短期事件库和见闻库索引，但保留必要的全局原始事件档案、调试日志或存档数据。总结请求和应用期间优先级最高，不可被守备官对话、NPC 闲聊、计划重评估或指令发布打断。

首次睡眠反思从上述种子继续生长：日记追加在种子之后；只有当天真实亲历、见闻或程序状态改变 / 强化了某项认识时，才替换对应知识键。不得把初始历史无意义地重写成当天经历。

NPC 信息面板不直接铺开长期日记。事件库上方并排显示“当前计划 / 日记 / 知识”三个等宽入口；日记与知识图谱分别在详情弹窗阅读，并在总结覆盖知识键值或追加日记后刷新。“当前计划”把小时连续且行动、来源、目标和可见说明等展示语义一致的计划项合并为一个时间段；若计划说明与行动名完全相同，不重复显示第二行。合并仅影响玩家 UI，底层仍保留 24 个小时阶段。首次打开时从包含当前小时的可见时段之前第二个计划行动开始，使当前时段通常位于第三个行动位置；0 点 / 1 点不足两项时夹取到当天开头，完整计划仍可滚动阅读。事件库 / 见闻库详情首次打开并完成排版后，默认滚到最底部的最新记录；已打开弹窗的增量刷新保留读者当前位置，不强制跳回底部。

## 11.9 知识图谱

知识图谱以对象 + 属性方式记录 NPC 对人物、地点、承诺和事件的认知。

内部 `subject / relation / value` 可以使用稳定技术键 / 值，但玩家界面必须显示中文主体名、关系名和当前值；模型同时提供 `subject_label / relation_label / value_label`，历史或未知技术内容由 UI 映射为中文保底。`confidence` 与记录时间继续保存在运行态和后端参数中，供替换、调试与后续检索使用；NPC“知识”面板不显示这两项内部元数据，只呈现角色实际知道的内容。

示例：

```json
{
  "schema_version": "key_value_replace_v1",
  "updated_day": 0,
  "updated_time": "开局前",
  "by_subject": {
    "guard_officer": {
      "role": {
        "value": "station_defense_alert_and_emergency_staff_coordination",
        "confidence": 0.9,
        "subject_label": "守备官",
        "relation_label": "职责",
        "value_label": "守备官负责统筹驿站防务、日常警戒，以及危急时站内人员的安排。",
        "day": 0,
        "time": "开局前"
      }
    }
  }
}
```

初始图谱从每名 NPC 自身视角记录其余 7 名在站人物、守备官、全部正式建筑及少量个人故事主体。本职建筑认识更具体，其他建筑粗略但必须符合程序规则；广场与公告牌不是建筑。守备官条目只写该职位统筹防务、警戒与危急人手安排等基本职责，不写品格评判、信任倾向、未来观察条件或既往互动。建筑规则必须包装成角色在驿站生活中会自然形成的常识，避免直接罗列初始数量、升级数值、槽位或系统流程。

## 11.10 第一人称日记

日记记录 NPC 主观感受，与知识图谱不同，它强调语气、情绪和人格连续性。

初始日记与后续反思使用完全相同的 8 字段运行时记录格式；固定 `day=0 / source=initial_long_memory / model_provider="" / model_name="" / model_fallback_used=false / debug_reason=seeded_before_game`，时间标签依次为“往昔·来站前 / 往昔·初到驿站 / 往昔·近日”。空 provider 与 `model_fallback_used=false` 表示种子不是模型生成或降级伪装。“往昔·来站前”用一个宏观切片勾勒身世、职业形成、离开旧生活的原因和来到驿站的时间；“往昔·初到驿站”写到站后接下的工作与当时已经在站的人，八名 NPC 的视角必须能拼成一致的到站顺序和驿站逐渐恢复生活机能的共同叙事；“往昔·近日”才使用具体、微观的普通驿站日常，并严格发生在守备官收到并向众人传达敌情之前。每篇简短、有个性、直白自然；可用少量容易理解的黑色幽默，但不能靠晦涩比喻、拟人或格言式收尾制造“余味”。日记不写成第三人称人物小传，也不复述知识图谱的客观规则清单。

运行态与 UI 始终保留上述 8 字段记录；发给 LLM 时不另造第二套日记结构，而是投影为带时间前缀的 `list[String]`：开局前记录形如 `往昔·近日：正文`，后续记录形如 `第2天 22:14:00：正文`。前缀用于判断先后，不属于 NPC 写下的正文；反思模型只输出正文，由 Godot 写入权威日期和时间。

示例：

```text
近日我重新核对了粮食、柴火和每个人的饭量。伊沃送来的菜比上周多一筐，马塞尔又问能不能留些粮酿酒。我让他先把晚饭吃完，再讨论酒窖。今晚的汤够所有人添一次；谁想添第二次，明天就来帮我刷锅。
```

## 11.11 记忆压缩策略

为避免上下文爆炸，每次 LLM 调用不直接注入完整原始事件历史；当前 Demo 注入压缩后的运行态长期记忆与下列必要上下文：

- NPC 人设核心
- 基础场景 `station_context`：所有携带 NPC 人设的请求都必须提供且各目录非空；包含精简驿站简介、全体登记 NPC 及每人的已入伍 / 在站标签、驿站全部建筑、工作模式下完整行为类型和精简驿站规则。规则以世界内口吻说明成员平时在站内工作生活、活动需持续参与并在周期完成后才结算、敌袭时入伍且持主武器者守站、其余人避敌，以及低士气时任何人都可能离站或临阵脱逃。人员、建筑、行为均来自运行时正式系统；离站者保留在基础名册中并标明不在站，不补造名单外内容；这些背景不替代单次行动候选或程序权威事实
- 当前状态
- 当天短期记忆摘要
- 当前 Demo 阶段注入该 NPC 的完整当前知识图谱和带时间前缀的日记文本，保证 15 座建筑、人物关系和日记先后在任何业务路径都可访问；同一请求只保留一个规范长期记忆字段，不在 target / speaker 或兼容镜像中重复
- 后续长期记忆明显增长时，可按当前对话对象、地点、任务和高重要性做可验证检索或压缩，但不得丢失程序规则边界，也不得泄露其他 NPC 的私人长期记忆
- 与玩家相关的高重要性记忆
- 当前守备官指令 `current_order`

---

# 12. 征召系统

## 12.1 征召前状态

开局只有副官可接收守备官指令。其他 NPC 由自动计划 + LLM 控制。

未入伍 NPC 可以自动工作、吃饭、睡觉、聊天、祈祷、治疗、逃离或避战。玩家可以与其对话，但不能向其发布个人指令。

## 12.2 对话中的“同意应征入伍”

玩家与 NPC 对话时，可以通过语言提出应征要求。对话窗右上角的“提出应征” toggle 是当前会话内的持续选项：开启后发送的消息会带上应征标记，发送后 toggle 保持开启，后续消息默认继续带标记，直到玩家主动关闭或本场会话结束。NPC 只有在收到这类已标记消息时，才会在该轮回复中选择是否同意应征入伍。第一次已标记消息发送后，本场会话不可取消；即使玩家随后关闭 toggle，仍只能完成或挂起，不能丢弃已经提出应征的会话。玩家只是打开对话窗、查看历史或关闭窗口，不会提出应征。

对话请求输入以目标 NPC 为中心：顶层唯一 `station_context` 提供精简驿站简介、动态在站名单、完整建筑 / 工作行为目录和精简驿站规则；`npc_id`、`npc_name`、`npc_setting`、`npc_state`、`current_order`、`short_memory`、`long_memory` 和 `location_context` 描述目标 NPC 及其所知事实；`speaker_name`、`speaker_text` 和 `speaker_context` 描述本轮说话者。玩家说话者名称固定为“守备官”；NPC 作为说话者时，只输入其当场可观察的健康/受伤状态和外表特征，不输入说话者的记忆、知识图谱、日记、私人指令、个人资源或地点私有上下文。`dialogue_state.visibility` 决定该轮对话后续入库时是 `private` 还是 `local_public`。完整行为目录只说明世界里有哪些工作模式行为；对话本轮可尝试能力仍由动态 `allowed_actions` 限定。`talk_to_npc` 候选只给出目标身份，不暴露其实时地点、行动或入伍状态；其他候选中的程序路由元数据也不自动成为目标 NPC 见闻。

NPC 根据以下因素决定是否同意：

- 人设与职业
- 当前生命、饱食、疲劳
- 与玩家互动形成的长期和短期记忆
- 地点见闻与广场公开信息
- 与其他 NPC 交流形成的长期和短期记忆

NPC-NPC 对话先经过受邀方接受 / 拒绝判定；邀请回复不计入正式轮次。接受后正式对话没有程序硬性轮次上限；每一轮回复者的文本只在没有输出结束标记时作为下一轮输入给另一名 NPC。模型每轮都可在回复文本之外输出结束标记：事情谈完即可结束；第 6 轮起若无紧急 / 必要事项，应自然告别并结束，有必要时允许继续。任意一方输出结束标记后，其回复作为最后一句直接入库，不再请求另一方回复。邀请被拒绝或正式对话结束后，实际参与的双方都分别依据本轮对话和各自原计划进入计划修改判别；空 `revision_hours` 保持原计划，非空才修订各自被选中的精确阶段。受邀者在邀请拒绝前没有被打断行动，因此空判别时继续原行动 / 工位。

## 12.3 征召结果

| 结果 | 说明 |
|---|---|
| 接受 | NPC 成为可接收守备官指令的可引导单位 |
| 拒绝 | NPC 保持自由行动 |

每次已标记应征消息收到明确结果后，对话历史在产生结果的 NPC 回复正下方显示一行系统反馈：接受为绿色“✓ XX接受了守备官的应征请求”，拒绝为红色“× XX拒绝了守备官的应征请求”。该反馈来自结构化 `recruitment_result`，不属于 NPC 的 `reply_text`，也不替代 NPC 自己说出的回复。

## 12.4 入伍后控制方式

NPC 应征后并非变成完全 RTS 单位，而是变成“可引导单位”。玩家可以打开其指令面板，自由撰写、查看和修改一条持续生效的当前指令，例如要求工作、训练、休息、治疗、协助修复或在战斗中守住某处。

该指令会进入后续对话、计划、计划重评估和战斗判定的上下文，但不会像 RTS 命令一样直接覆盖行动。NPC 仍会根据人格、记忆、现场状态和程序强制规则理解或拒绝指令。玩家仍可直接分配武器、盔甲和坐骑；资源、HP、行动合法性和战斗结算由程序决定。

---

# 13. 装备、兵种与战斗定位

## 13.1 兵种不是硬编码职业

兵种由装备与坐骑决定，而非角色职业决定。

| 装备组合 | 实际兵种 |
|---|---|
| 剑盾 | 近战步兵 |
| 长杆武器 | 长杆步兵 |
| 弓 | 弓箭兵 |
| 弩 | 弩兵 |
| 近战武器 + 马 | 近战骑兵 |
| 远程武器 + 马 | 骑射单位 |
| 无武器 | 非战斗人员 / 避战单位 |

## 13.2 装备部位

每个入伍 NPC 可装备：

- 主武器，限制为一种
- 头盔
- 胸甲
- 腕甲
- 腿甲
- 坐骑，可选

装备会改变外观。进入战斗状态后，NPC 会显示当前武器和盔甲。装备与部署必须消耗相同具体 ID 的库存：剑盾库存不能换成弓，铁盔库存不能换成锁子甲，弩床库存也不能部署箭塔；收回或替换装备时，只返还原具体物品。

马匹分配规则：

- 只有已入伍且已经装备主武器的 NPC 才能分配马匹；这里的“有装备”明确指具备战斗资格的主武器，仅有盔甲不满足条件。
- 只能分配成年、未被其他 NPC 分配且物理上位于马厩的马；每名 NPC 至多一匹，每匹马至多分配给一名 NPC。小马不可分配。
- 分配只建立预留关系，日常 / 工作模式下马仍留在马厩并参与进食、恢复和马厩数量统计。
- 集结与战斗统称战时骑乘阶段。NPC 进入 `rally` 或 `combat` 后自动上马，马离开马厩并被骑乘；退出战时阶段后返回马厩，分配关系保留。
- 收回主武器、取消入伍、逃离驿站或其他导致 NPC 不再具备分配资格的变化，会自动取消马匹分配并清空坐骑槽；仅更换主武器或收回盔甲不取消。
- 马匹实体和分配关系由程序权威系统结算；装备槽只保存带 `horse_id` 的兼容快照，不能复制出另一匹马。

## 13.3 熟练度与战斗表现

| 因素 | 影响 |
|---|---|
| 武器熟练度 | 攻击速度、命中、特殊动作效率 |
| 力量 | 攻击力、生命上限、负重、部分移动速度 |
| 骑术 | 骑行速度、冲锋威力、骑射稳定性 |
| 盔甲 | 降低伤害，提高负重和疲劳消耗 |
| 士气 | 影响伤害修正、逃跑概率、斗志激昂概率 |
| 疲劳 | 降低攻击速度、移动、工作效率 |
| 饱食度 | 影响持续作战和状态恢复 |

训练场提升的武器熟练度和骑术会作为后续战斗表现的输入，但训练本身不直接决定伤害、命中或资源等权威战斗结算。

---

# 14. 战斗系统

## 14.1 战斗触发

战斗系统围绕行为模式切换推进，而不是在“战斗触发”时让所有人同时做一次心理判定。

触发入口：

- 守备官手动摇响警铃：符合条件的已入伍持武器 NPC 进入集结模式，前往城门外防线；未入伍、无主武器、昏迷、睡觉或不可行动 NPC 不进入集结。
- 集结模式接敌：NPC 在集结途中或集合点等待时，敌人进入一定范围，立即进入战斗模式。
- 工作模式接敌：已入伍且有主武器的 NPC 在日常 / 工作模式中，敌人进入一定范围，自动进入战斗模式；正在睡觉的已入伍持武器 NPC 只有被敌人攻击时才从睡觉直接进入战斗。
- 非战斗人员遇敌：未入伍 NPC、已入伍但无主武器 NPC 都进入避战模式；正在睡觉的非战斗人员只有被敌人攻击时才从睡觉进入避战。

集结模式到达集合点后开始等待计时。若等待 1 游戏小时仍未接敌，该 NPC 返回工作模式并按当前计划继续执行，不调用计划重评估。

当场上敌人全部消失后：

- 处于战斗模式的 NPC 返回工作模式，并重新评估计划。
- 处于避战模式的非战斗人员返回工作模式；除非避战期间发生受伤、对话、应征等额外异常，否则不因单纯避战结束自动调用 LLM 重评估。
- 处于集结模式且尚未接敌的 NPC 可结束集结并返回工作模式，不触发计划重评估。

工作 / 战斗、工作 / 避战的互转属于运行时状态变化，不再作为 `npc_mode_changed` 事件写入或广播；战斗伤害、攻击、避战开始 / 结束等事实事件继续保留。

战斗数值不再绑定玩家时间倍率。伤害、攻击间隔、攻击速度和战斗移动速度按自身配置、属性、熟练度、装备、防御和战斗状态计算；玩家选择 `x2` / `x4` 不会让攻击或移动变快。敌人在场时通过 TimeSystem 的 `x1` 上限防止世界逻辑时间过快；战斗中的 LLM 调用仍可把有效时间进一步降到慢速，调用结束后回到敌人在场的 `x1` 上限。攻击冷却和 `attack_interval` 使用战斗动作秒，避免默认 `x1` 下现实 1 秒推进 60 游戏秒时发生几十次攻击。

## 14.2 战时对话与心理结果

取消旧规则：不再在战斗触发时让全员进行一次战斗前心理判定。

已入伍且有主武器的 NPC 在集结模式和战斗模式下，守备官都可以主动发起对话。该对话与日常模式对话不同：

- 对话强制为同地点公开，UI 中“同地点公开”默认开启且不可关闭。
- Prompt 必须明确 NPC 当前处于集结模式或战斗模式，并注入对应的集结 / 战斗事件。
- 对话上下文继承 T0603 的 NPC 设定、状态、短期记忆、长期记忆、地点上下文和当前 `current_order`，并额外加入当前战局上下文：场上敌方和友方数量、兵种、HP 概况，哪些 NPC 正在参战，哪些 NPC 仍在驿站但不是战斗人员。
- LLM 回复在普通对话输出外，增加战时心理结果字段，例如 `wartime_reaction`：`none` / `escape` / `morale_boost`。

若守备官说得好，NPC 可能获得斗志激昂；斗志激昂是程序状态 buff，在接下来的 2 游戏小时内提高一定攻击力和移动速度。若守备官说得糟，NPC 可能触发逃离驿站。LLM 只输出意向，buff 数值、持续时间、逃离移动和事件入库由程序结算。

非战斗人员在避战模式下也可以与守备官对话：

- 对话同样强制为同地点公开，公开 toggle 默认开启且不可关闭。
- Prompt 必须明确 NPC 正在躲避敌人袭击，并注入同样的战局上下文。
- 守备官仍可勾选“提出应征”；NPC 是否同意应征沿用日常对话的征召结果逻辑。
- 避战对话不直接进入战斗心理判定；如果 NPC 在避战中同意应征但仍无主武器，继续避战；只有在已入伍且装备主武器、场上仍有敌人时，程序才让其进入战斗模式。

## 14.3 低血量自身心理判定

当前战斗 / 敌人在场期间，当某个 NPC 的生命值首次从不低于 30% 跌破 30%，且仍大于 0 时，触发一次 LLM 判定。该规则覆盖所有未昏迷、未逃离的 NPC，不只覆盖已经参战的人：避战中的非战斗人员也可能被敌人追上、打到残血并产生逃离或继续躲避的判断。

触发范围：

- 已入伍且有主武器、实际处于战斗模式的 NPC，会按“继续参战 / 逃离 / 斗志激昂”处理。
- 未入伍 NPC、已入伍但无主武器 NPC、以及处于避战模式的非战斗人员，也会触发低血量判定，但不允许获得斗志激昂，也不会因此继续参战；他们只能产生逃离驿站意向，或留在驿站继续避战。
- 已昏迷、已逃离、HP 已在 30% 以下后再次受击、或 HP 直接清零进入昏迷的 NPC 不触发该判定。
- 没有守备官本轮发言；请求基于与日计划 / 计划判别 / 正式修订一致的 NPC 人设、权威状态、当前 `current_order`、亲历 / 见闻短期记忆、知识图谱 / 日记长期记忆、地点上下文，再结合战局上下文和低血量事实。记忆与指令只影响主观判断，不能覆盖程序确认的 HP、装备、入伍、行为模式和 `allowed_decisions`。
- 每名 NPC 每波或每场战斗最多触发一次，避免频繁调用。

参战 NPC 的可能结果：

- 继续参战
- 逃离驿站
- 斗志激昂

不参战 / 避战 NPC 的可能结果：

- 逃离驿站
- 留在驿站继续避战（无事发生）

判定等待期间，守备官不能与该 NPC 对话。如果守备官正在与该 NPC 对话而低血量判定触发，当前对话立即被强制中断并关闭，未完成的对话 LLM 请求被取消，NPC 进入自身心理判定。自身心理判定同样申请 TimeSystem 慢速，返回、失败或规则降级后释放。

若该单位继续受到伤害并导致 HP 清零，则不会死亡，而是进入昏迷状态。

## 14.4 昏迷、治疗与复苏

战斗中 NPC 的 HP 清零后，会在原地倒下并进入昏迷状态。

规则如下：

- 昏迷 NPC 不再被视为可战斗单位。
- 昏迷 NPC 会以极慢速度自然恢复 HP。
- 其他NPC可以治疗昏迷 NPC，使其更快恢复 HP。
- HP 恢复到 30% 后，NPC 复苏并重新进入可行动状态。
- 若场上仍有敌军，复苏后的已入伍且有主武器 NPC 进入战斗模式，未入伍或无主武器 NPC 进入避战模式。
- 若场上没有敌军，复苏后的 NPC 回到工作模式并重新评估计划。
- 昏迷、治疗、复苏事件会实时写入战场事件，并通过广场信息节点即时广播给当前在场 NPC。

## 14.5 战斗策略

### 14.5.1 剑盾/长杆步兵

- 主动进攻：主动上前攻击最近敌人。
- 避战：按敌人来袭方向短距离远离；敌人已远离到安全阈值外时站立等待，不主动逃离地图或退到驿站角落。

### 14.5.2 弓/弩步兵

- 最大化输出：站定射击。
- 保持距离射击：保持小于攻击距离的安全距离。
- 避战：按敌人来袭方向短距离远离；敌人已远离到安全阈值外时站立等待，优先保命且不主动射击。

### 14.5.3 近战骑兵

- 主动进攻：主动上前攻击最近敌人。
- 拉开距离冲击：先拉开距离，再冲刺攻击造成更高伤害，随后重新拉开距离。
- 避战：按敌人来袭方向短距离远离；敌人已远离到安全阈值外时站立等待，避免接敌。

### 14.5.4 骑马远程

策略同远程步兵：最大化输出、保持距离射击、避战。

战斗策略中的“避战”只适用于已入伍且有主武器 NPC 的战斗模式，代表在战斗中复用非战斗人员避战的短步长远离逻辑、优先自保，但仍属于战斗模式。它不是非战斗人员的同级避战模式；敌人已经远离到安全阈值外时，NPC 应站立等待，不继续向驿站边界或角落移动。

## 14.6 非战斗人员避战模式

未入伍 NPC、以及已入伍但没有主武器的 NPC 都属于非战斗人员，不进入集结模式和战斗模式。敌人进入其一定范围内时，非战斗人员进入避战模式：系统根据最近敌人的接近方位生成短距离远离目标，并为不同 NPC 加入不同散射方向，形成四散逃跑效果；NPC 会尝试逐步远离到接敌范围之外，但不会一步挪到驿站角落，也不会离开驿站太远。

避战模式不同于逃离驿站：

- 避战只是在驿站内躲避敌人，目标是活下来并等待危险结束。
- 逃离驿站是 NPC 决定离开据点，前往后门或小门离开地图。
- 避战中的 NPC 只有在场上没有敌军后才返回工作模式。
- 避战中若通过对话同意应征但仍无主武器，继续按非战斗人员避战；若场上无敌军，则回到工作模式，后续可接收指令和装备；若已入伍且获得主武器且场上仍有敌军，才转入战斗模式。

## 14.7 敌人 AI

Demo 阶段敌人使用规则 AI，不调用 LLM。

敌人目标优先级：

1. 如果一定范围内有我方单位，优先攻击我方单位
2. 攻击城门
3. 城门被攻破后攻击仓库
4. 仓库被摧毁后攻击主厅

当前规则不把围墙作为敌人攻击目标。后续“敌人必须从城门口进入、不能直接穿越围墙”的空间约束，交给可进入 / 不可进入对象碰撞体积、导航路径或类似阻挡系统实现。

敌人类型使用与我方相同的武器逻辑：

- 近战步兵
- 长杆步兵
- 弓箭兵
- 弩兵
- 骑兵

## 14.8 战场公开信息

战斗中的关键事件会实时写入结构化事件系统，并通过广场信息节点即时广播给当前在场 NPC。

公开信息包括：

- 对方大致有多少人
- 我方大致有多少人
- 某个 NPC 被打到 30% HP 以下
- 某个 NPC 击倒或击杀敌人
- 某个 NPC 昏迷
- 某个 NPC 被治疗
- 某个 NPC 从昏迷中复苏
- 某个 NPC 离开战场或逃离驿站
- 城门、仓库、主厅等建筑受到攻击或受损

战斗结束后，NPC 回到工作状态，并根据战场公开信息、自身经历和短期记忆重新评估计划。

---

# 15. 逃离驿站系统

## 15.1 逃离触发

NPC 可能在以下情况下选择逃离：

- 入伍前在驿站不开心或害怕被进攻而逃离
- 集结 / 战斗模式下，守备官主动对话说服失败，战时心理结果为逃离
- 战时 HP 低于 30% 的自身心理判定结果为逃离；该来源覆盖参战 NPC 和避战中的非战斗人员
- 看到其他 NPC 昏迷、复苏失败或逃离
- 广场公开信息显示战况极度不利

非战斗人员的避战模式不是逃离驿站。避战只在驿站内部远离敌人；只有当后续 LLM 判定或对话结果明确触发 `escape` / 逃离意向时，才进入逃离驿站流程。

## 15.2 逃离流程

当 NPC 进入“逃离驿站”行动时，会前往地图中的小门或后门。其路径与速度由程序控制。

在 NPC 完全离开地图前，玩家可以进行干预：

- 点击逃离 NPC 后先打开 NPC 面板，再点击【对话】进入最多 5 轮逃离挽留对话
- 承诺补偿
- 威胁处罚
- 赠予金钱或资源
- 对其造成伤害以示威胁

进入逃离挽留对话时，NPC 暂停逃离移动；玩家发送消息并收到 NPC 回复计为 1 轮。若 5 轮内挽留成功并完成会话，NPC 停止逃离；若 5 轮都未挽留成功，系统自动完成会话并关闭面板，NPC 继续逃离，NPC 面板的【对话】按钮变为灰色不可点击。“取消对话”结束本场挽留且不应用未提交的挽留意向、不写对话事件，NPC 立刻恢复逃离；“挂起对话”只隐藏窗口，NPC 仍保持暂停，头顶和 NPC 面板显示未结束标记，最多 2 个游戏小时后自动取消。只要 5 轮额度未用完，完成或取消后仍可再次发起新的挽留会话。

逃离中的 NPC 需要有明显警示：头顶显示特殊标识，主 UI 显示有 NPC 正在逃离。逃离中给钱会让其离开的移动速度变慢；守备官攻击会让其离开的移动速度变快。逃离挽留对话里的攻击按钮会计为 1 轮并锁定取消，点击后不向 NPC LLM 发送消息、不产生 NPC 回复，而是以守备官攻击行为作为最后一句自动完成并保存本场会话，然后关闭面板、继续逃离。若 NPC 在逃离期间昏迷，逃离意图不会被取消，复苏后依然继续逃离驿站。

伤害、给钱、挽留结果、NPC 离开驿站或尝试逃离的事件会被记录，并通过广场信息节点即时广播给当前在场 NPC。LLM 只输出文本和结构化意图，不直接改变 HP、资源、移动速度或逃离终点。

## 15.3 挽留结果

逃离挽留只允许两类结构化结果：

- 留下：NPC 停止逃离，返回工作模式，并重新做计划。
- 继续逃离：NPC 继续朝出口移动；若 5 轮内都未留下，玩家不能继续发起挽留对话，【对话】按钮置灰。

当前设计不包含“留下但退出入伍状态”分支。攻击导致昏迷也不等于取消逃离；NPC 复苏后继续逃离。

---

# 16. 商人系统与经济补给

## 16.1 商人到访

当前 Demo 中，商人马车每天 10:00 到达后门，16:00 离开。到访时段与报价由 `data/merchant_defs.json` 配置，后续平衡不应把数值写死在 UI。玩家可以用第纳尔购买资源，也可以出售酒换钱；商人未到、余额不足或库存不足时交易失败且资源不变。

## 16.2 可交易内容

| 交易方向 | 资源 | 当前单价（第纳尔） |
|---|---|---:|
| 驿站买入 | 粮食 | 2 |
| 驿站买入 | 木材 | 3 |
| 驿站买入 | 石料 | 4 |
| 驿站买入 | 铁 | 5 |
| 驿站卖出 | 酒 | 7 |

酒窖完成酿酒时只增加驿站酒库存，不自动换钱。酒既可等商人到访后由交易系统扣除并增加第纳尔，也可由守备官从 NPC 面板赠予个人；赠酒会从驿站库存转入 NPC 个人持有量。到达、离开、成功交易和赠酒均形成结构化事件，并按各自可见性进入对应事件 / 见闻。

---

# 17. 胜负与结算

## 17.1 失败结算

失败后进入结算页面，显示：

- 驿站失守原因
- 主厅摧毁时间
- 每名 NPC 的最终状态
- 每名 NPC 的个人结局

## 17.2 胜利结算

守住 5 波敌人后进入 Demo 胜利结算，显示：

- 驿站是否仍可运转
- 剩余资源
- 建筑损毁情况
- 存活、昏迷、逃离 NPC 列表
- 每个 NPC 对玩家的最终看法
- 每个 NPC 的后续命运
- 玩家获得的结局称号

---

# 18. AI / LLM 系统设计

## 18.1 模型调用原则

LLM 负责“人如何理解和回应事件”，不负责最终权威数值结算。

程序负责：

- HP 扣除
- 昏迷、治疗和复苏状态判定
- 资源扣除
- 建筑受损
- 产出计算
- 寻路
- 战斗命中与伤害
- 是否可执行行动
- 广场公开信息写入

LLM 负责：

- NPC 为什么这么做
- NPC 如何说话
- NPC 对玩家行为的主观解释
- NPC 是否愿意征召的意向
- NPC 是否想逃跑、参战、崩溃或斗志激昂
- NPC 是否主动找玩家交涉
- NPC 对昏迷、治疗、复苏、逃离等事件的理解
- 首次睡眠日记和记忆压缩

## 18.2 结构化输出要求

所有关键 LLM 调用必须返回 JSON，不允许只返回自然语言。

对话输出分两类：回复守备官时返回回复者 ID、回复文本和 `recruitment_result`（`none` / `accept` / `reject`）；回复 NPC 时返回回复文本和是否结束对话。若对话发生在集结 / 战斗 / 避战模式，还必须带上当前模式和战局上下文，并按模式返回额外结构化意向：已入伍且有主武器 NPC 在集结 / 战斗对话中返回 `wartime_reaction`（`none` / `escape` / `morale_boost`），避战模式下的非战斗人员继续使用 `recruitment_result` 表达是否同意应征。LLM 输出只表达文本与意向，征召状态切换、斗志 buff、逃离移动、事件入库、公开广播和任何资源/HP/建筑变化仍由程序执行。

## 18.3 模型选择原则

模型应满足：

- 中文角色扮演稳定
- JSON 输出稳定
- 成本可控
- 支持较长上下文
- 延迟可接受
- API 可用性稳定

候选包括 DeepSeek、MiniMax、通义千问、智谱等国内性价比较高模型。最终不应把游戏逻辑绑定在某个具体模型上，而应设计统一 Model Adapter，以便替换供应商。

## 18.4 额度计算系统

需要在后端记录模型调用次数、调用用途、输入输出 token、对应 NPC、对应事件和费用估算。

## 18.5 LLM 等待与逻辑时间减速

每个会影响 NPC 行为的 LLM 请求，在 Godot 发起请求时应向 TimeSystem 注册一个等待请求；请求成功、失败、超时或规则 / 模板降级后必须释放该请求。

等待期间：

- 玩家仍可操作镜头、查看面板、阅读对话或进行允许的 UI 操作。
- 游戏逻辑时间减缓为游戏内 1 秒 = 现实中 1 秒。
- 工作 / 日常状态结算使用减速后的有效倍率；战斗若正在进行，也会因全局慢速而放慢推进，但战斗数值不读取玩家加速倍率作为额外倍率。
- 不修改 `Engine.time_scale`，不让 NPC 移动、动画和物理整体慢放。
- 后端失败或超时必须释放减速请求，并返回可处理错误或回退规则 / 模板结果。规则 / 模板降级必须标明来源并保留真实失败原因；不得用 mock 内容伪装真实模型成功。

## 18.6 Mock、真实 API 与失败可见性

Mock 是开发期工具，用于快速验证 Schema、前后端通信和自动化脚本。它不属于成品游戏的模型失败兜底。

- 任何 Prompt 或 LLM 业务路径，都应先用 mock 跑通基础测试，再用真实 API Key 发起真实 provider 测试。
- 如果没有真实 Key 或真实 provider 测试未完成，不应把该 LLM 行为标记为最终完成。
- 真实 provider 失败、超时、无 Key、非 JSON 或 Schema 校验失败时，系统必须能在日志 / usage 中看到 request id、call_type、provider、model、相关 NPC、HTTP 状态或异常类型和失败原因。
- 允许规则 / 模板降级保持其他已明确允许降级的游戏流程可玩，例如睡眠总结使用模板、战斗心理按允许结果保守处理；但这些结果必须标明是降级结果，不能写成模型成功。正式每日计划和行动失败后的正式计划重估是例外：两者只接受真实 provider 成功结果，失败时分别保持计划阶段暂停或保留原计划，不使用 Mock / 规则计划继续伪装成功。
- 成品 / Demo 默认配置应关闭自动 mock fallback。Mock 只能通过显式开发模式或调试接口启用。

---

# 19. 并发与性能策略

## 19.1 调用优先级

| 优先级 | 任务 |
|---|---|
| 高 | 玩家正在对话的 NPC |
| 高 | 集结 / 战斗 / 避战模式下的战时公开对话 |
| 高 | 守备官发布新指令后立即触发的计划重评估 |
| 高 | 战时 HP 低于 30% 的自身心理判定 |
| 中 | 逃离挽留对话 |
| 中 | NPC 主动找玩家交涉 |
| 中 | 行动异常重评估 |
| 低 | NPC 间闲聊 |
| 最高 | 首次睡眠总结 |
| 低 | 每日计划，可批处理 |

## 19.2 LLM 等待并发与减速规则

Godot 侧为每次模型请求生成唯一 `request_id`。TimeSystem 保存当前等待中的请求集合，并以集合中最慢倍率作为有效逻辑倍率。

- 玩家主动对话、征召、集结 / 战斗 / 避战公开对话、低血量自身心理判定、逃离挽留等高优先级请求可立即申请减速。
- 每日计划按 8 名初始 NPC 同批最多 8 路并发，正式开局和跨天都遵守“全部成功后再执行”的事务边界。首次睡眠总结虽然频率低，但同一时刻多名 NPC 达到条件时也最多 8 路并发；总结在对应 NPC 当下状态上不可打断，优先级高于对话和指令。
- 所有 NPC 相关 LLM 调用都必须申请减速，包括对话、每日计划、计划修订、主动交涉、战时公开对话、低血量自身心理判定、逃离判定和首次睡眠总结。
- 所有请求必须有超时释放，避免游戏长期卡在慢速逻辑时间。

---

# 20. 语音输入与情绪识别

## 20.1 玩家语音输入

玩家可用语音输入对 NPC 说话。语音转文字后进入对话系统。

## 20.2 情绪识别

语音识别模型需要尽量提供情绪识别或可另接情绪分类模型。情绪结果可作为玩家话术的修正项。

示例：

| 玩家语气 | 影响 |
|---|---|
| 平静 | 正常说服 |
| 愤怒 | 更像威胁 |
| 低沉 | 更像安抚或疲惫，可能提升真实感 |
| 急促 | 传达危机，提高紧张感 |
| 嘲讽 | 提高反感概率 |

Demo 阶段可先实现文字输入，语音输入作为增强项。若时间不足，情绪识别可延期。

---

# 21. UI 与信息展示

## 21.1 主界面

主界面为俯视 3D 驿站地图。需要显示：

- 时间与天数
- 当前波次倒计时
- 资源栏
- 建筑状态提示
- NPC 头像/状态简表
- NPC 主动交涉问号气泡
- NPC-NPC 自主对话的双方小气泡与可点击旁听入口
- 当前模型调用状态，可开发调试用
- 加速按钮
- 警铃按钮

HUD 时钟正常 `x1 / x2 / x4` 下显示 `HH:MM:00`，LLM 慢速期间恢复实际秒；当前波次及其他持续倒计时使用同一显示精度，避免正常速度下快速跳字。

## 21.2 NPC 面板

点击 NPC 显示：

- 姓名
- 人物背景按钮，位于姓名旁；点击后显示与 NPC Prompt 相同的外貌、背景故事、职业背景、性格、欲望、恐惧、底线、说话风格和能力，不维护独立 UI 文案
- 当前行动
- HP、饱食、疲劳、士气、个人金钱与个人持酒数量
- 是否昏迷
- 主要熟练度
- 当前装备
- 当前生效的 24 小时计划入口；新一天计划或计划重估后覆盖刷新
- 当天事件库和见闻库应分开显示，点击后可打开更大的详情弹窗，但玩家详情仍只展示与小框相同的时间和事件摘要，不暴露事件 ID、内部类型、参与者 ID 或 payload；首次打开在排版完成后默认滚到最底部的最新记录，已打开时的增量刷新保留玩家当前阅读滚动位置
- 第一人称日记与知识图谱入口；日记不再附带独立记忆摘要，知识图谱只向玩家显示叙事化的中文主体 / 关系 / 值，不显示内部可信度和更新时间
- 对玩家态度
- 对话按钮；右侧另有较小的“记录”按钮，只读查看当前 NPC 与守备官已经完成的过往会话
- 赠予金钱按钮
- 赠予酒按钮与数量输入，紧邻赠予金钱入口右侧
- 给予/更换装备按钮；因尚未入伍而禁用时，悬停应明确提示必须先说服该人物应征入伍
- 马匹下拉框与分配 / 取消入口；仅在 NPC 已入伍且持有主武器时可用，只列出成年、未分配且当前在马厩的马，并显示不可用原因
- 指令按钮，若已入伍；打开自由文本指令撰写与发布面板

NPC 入伍后，主场景头顶姓名与 NPC 面板姓名统一显示为淡绿色；HP、当前行动等状态文字保持中性色，未入伍姓名保持原色。

“记录”复用全局事件档案中的守备官会话，不建立第二套谈话数据库，也不受熟睡总结清空单人短期索引影响。记录按事件自身日期、时间与当时所处的战斗波次阶段分组，保留每场完整说话者和正文；NPC-NPC 自主对话不混入。打开记录不打断 NPC、不创建会话、不触发 LLM 或计划重估。

## 21.3 建筑面板

点击建筑显示：

- 等级
- HP
- 当前工作位
- 当前产出
- 储存或消耗
- 升级条件
- 修复按钮
- 当前地点信息
- 宿舍床位只显示逐项实时状态：`床位X：空闲` 或 `床位X：XX占用中`，不显示固定归属
- 围墙专属的工程器械库存、类型、可用槽位与已部署器械

修复 / 升级进行中时，正文和按钮以 `x小时x分xx秒` 显示剩余时间；正常速度秒位为 `00`，LLM 慢速期间显示实际游戏秒。

铁匠铺和工械坊必须额外显示目标下拉框、配方材料、阶段数、当前阶段名、具体成品库存和总进度条。总进度按 `(已完成阶段 + 当前有效工作周期比例) / 总阶段数` 计算；没有目标时进度为 0，相关工作不可开始。进度不为 0 时选择其他目标，先把下拉框恢复到旧选项并弹出确认框，明确旧 / 新目标、当前阶段和“已投入材料不返还”；取消不改变任何状态，点击“是”才由制造权威系统中断旧周期、清空旧项目并更换目标。

马厩使用“在厩 / 离厩”总量摘要，不重复显示“马厩马匹”小标题；并用可滚动列表逐匹显示名字、成年 / 小马、拆分后的基础 HP、照料额外 HP、饱食度、成长、繁育概率 / 冷却、进食 / 恢复状态、分配对象和当前位置。逐匹信息不再另列与分配对象重复的骑手字段。建筑面板可直接读取马匹系统的完整玩家可见快照；这些个体详情不等于 NPC 可获取的建筑状态。

修复、升级等按钮的资源消耗和执行条件不应常驻堆在建筑面板正文中；面板正文优先显示建筑当前状态。玩家将光标悬停在修复或升级按钮上时，按钮旁应出现提示框，显示所需资源、是否资源不足、是否已满级或是否已有修复作业等条件。浮动提示不得参与对象面板的自然高度计算，面板刷新也不得先显示一帧最大高度空白区，以免在 NPC / 建筑面板下方形成闪烁黑框。

围墙部署界面只提交器械与槽位；库存扣除、槽位占用和自动攻击由工程器械权威系统结算，不选择或校验部署 NPC。器械正式模型、动画和特效属于表现层，可替换当前占位模型，不改变部署数据与战斗规则。

## 21.4 对话界面

对话界面应突出 NPC 表情、当前情绪、输入框和历史对话。

玩家点击发送后自己的消息立即显示，再等待 NPC 回复。所有主界面常用弹窗都应把最上方标题栏作为拖动把手，并在拖动及分辨率变化后限制在可用视口内；标题栏中的关闭、toggle 等控件仍保持各自点击行为。

应征接受 / 拒绝的结构化结果必须紧跟在对应 NPC 回复下一行显示：接受使用绿色对勾，拒绝使用红色叉号；普通回复不显示结果行，后续回合不得覆盖此前结果。

可选按钮：

- 提出应征 toggle，选中后本场后续发送默认持续提出应征，直到主动关闭
- 攻击按钮，攻击后 NPC 会回应惩戒
- 赠予金钱（后续可选）
- 完成对话：保存会话并触发必要的计划修改判别
- 取消对话：丢弃会话；NPC 主动交涉、发生攻击或已发送应征消息后置灰，主动交涉悬停显示不可取消原因
- 挂起对话：暂时隐藏并保留 NPC 对话占用，2 个游戏小时后自动收口

核心输入应支持自由文本，以突出 AI 玩法。

## 21.5 公告牌界面

公告牌使用“通告 / 日程表”双页签。通告页载入当前已发布文本作为可编辑副本，不重复显示已发布字数、草稿标题或默认退出说明；日程表逐行显示开始时间、结束时间、内容和删除按钮，并提供“+ 新建安排”。两个页签都只编辑本地草稿，退出 / 关闭一律丢弃，只有发布才把整页内容提交给广场状态，但界面不常驻重复解释这套机制。日程表页黄色区域直接显示非强制世界内备注；校验失败、重叠和发布结果仍按需反馈。

---

# 22. 数据结构建议

## 22.1 NPC Profile

```json
{
  "id": "horseman_01",
  "name": "待定",
  "background_job": "马夫",
  "personality": ["谨慎", "重感情", "怕死"],
  "desires": ["保护马厩", "活下来"],
  "fears": ["被派上前线", "马匹被征用"],
  "boundaries": ["不能接受无意义牺牲"],
  "stats": {
    "strength": 6,
    "intelligence": 4
  },
  "states": {
    "hp": 100,
    "satiety": 80,
    "fatigue": 20,
    "money": 2,
    "unconscious": false
  },
  "skills": {
    "养马": 70,
    "骑术": 45,
    "厨艺": 5,
    "剑盾": 0,
    "弓": 5
  },
  "recruited": false,
  "current_order": {
    "text": "",
    "issued_by": "guard_officer",
    "issued_day": 0,
    "issued_time": "",
    "revision": 0
  },
  "equipment": {},
  "knowledge_graph": {},
  "diary": []
}
```

T0061 后，正式 `data/npc_profiles.json` 中的 `personality / desires / fears / boundaries / background_story / speech_style` 均使用自然、直白、符合中文母语习惯的表达，并通过句式、用词、关注点与判断习惯区分角色。档案不再保存代表性台词或固定口吻样例，避免把后续对话锁进少数句式；`background_story` 只写人物最根本且稳定的职业观察方式、价值尺度与矛盾，不写具体往事、人物关系或建筑知识。基础档案中的 `knowledge_graph={}` 与 `diary=[]` 只是空运行字段占位。

开局前历史独立存放在 `data/npc_initial_long_memory.json`，以 NPC id 为顶层键：

```json
{
  "stableman_01": {
    "diary": [
      {
        "day": 0,
        "time": "往昔·来站前",
        "entry": "……第一人称生命切片……",
        "source": "initial_long_memory",
        "model_provider": "",
        "model_name": "",
        "model_fallback_used": false,
        "debug_reason": "seeded_before_game"
      }
    ],
    "knowledge_graph": {
      "schema_version": "key_value_replace_v1",
      "updated_day": 0,
      "updated_time": "开局前",
      "by_subject": {}
    }
  }
}
```

NPCSystem 只有在种子 id 与 8 人基础档案完全对应、日记和图谱结构有效时才生成 NPC；运行时后续反思追加日记并替换知识键，不回写基础档案或种子文件。

## 22.2 Event Record

```json
{
  "event_id": "evt_day03_090000_doctor_01_unconscious",
  "day": 3,
  "time": "09:00:00",
  "type": "unconscious_started",
  "subject_npc_id": "doctor_01",
  "actor_ids": ["enemy_03"],
  "target_ids": ["doctor_01"],
  "location_id": "plaza",
  "visibility": "local_public",
  "importance": 85,
  "summary": "莉娜在广场战斗中被敌人击倒并昏迷。",
  "payload": {
    "hp_before": 18,
    "hp_after": 0,
    "damage": 18,
    "weapon_id": "raider_axe"
  }
}
```

## 22.3 NPC Daily Memory

```json
{
  "npc_id": "doctor_01",
  "day": 3,
  "event_log": ["evt_day03_083000_doctor_01_location_entered", "evt_day03_090000_doctor_01_unconscious"],
  "witness_log": ["evt_day03_084500_stableman_01_escape_started"]
}
```

## 22.4 Location Info Space

```json
{
  "id": "chapel",
  "name": "小教堂",
  "kind": "enterable_building",
  "external_state": {
    "level": 1,
    "condition": "intact"
  },
  "internal_state": {
    "people_present": ["priest_01", "doctor_01"],
    "people_statuses": [
      {
        "npc_id": "priest_01",
        "life_status": "healthy",
        "life_status_text": "健康",
        "action_status": "待命"
      },
      {
        "npc_id": "doctor_01",
        "life_status": "unconscious",
        "life_status_text": "昏迷，马塞尔正在治疗",
        "healer_npc_ids": ["priest_01"],
        "action_status": "昏迷"
      }
    ],
    "workstations": [
      {"id": "altar", "status": "occupied", "occupied_by": "priest_01"}
    ],
    "special_state": {}
  },
  "current_public_note_ids": [],
  "state_snapshot": {
    "external_state": "外部状态",
    "internal_state": "内部状态"
  }
}
```

所有建筑都有可传播外部状态：等级、完好/受损/正在修复/正在升级、是否可进入和运行效率分档。HP、Max HP、精确效率与剩余修复/升级时长仍是程序权威数据，但不作为 NPC 见闻传播状态。广场和可进入建筑都会在进入快照中提供当前在场 NPC 的生命状态/行动状态。只有可进入建筑拥有位置内部状态：每个位置的稳定 ID、显示名、类型、占用/空闲状态和占用者；位置新增、移除或改名也触发在场人员的字段级差量。不可进入建筑不暴露内部 NPC、NPC 状态或位置状态。

`internal_state.special_state` 是按建筑类型列白名单的内部特殊状态：铁匠铺 / 工械坊只允许 `production.target_item_id / target_name / completed_stages / total_stages / current_stage_index / current_stage_name`；马厩只允许 `horses.total / adult / foal`，且只统计物理上在马厩的马。进入者在一次性快照中获得这些字段，已经在场且未昏迷 / 未睡觉的 NPC 只获得字段级差量。当前周期的小数进度和逐匹马的 HP、饱食、成长、进食、分配详情不进入这里，也不广播到广场。面板所需完整数据分别直接读取制造系统和马匹系统。

广场的 `kind` 为 `plaza`，没有建筑 HP；其 `state_snapshot` / `building_external_states` 继承所有建筑的可传播外部状态，而不是只继承不可进入实体。

进入/离开地点不应把 `state_snapshot` 复制进亲历事件库。`location_entered` / `location_exited` 只记录行动事实；进入者需要的地点状态作为单独见闻写入 `witness_log`。进入广场时，该快照包含当前广场在场 NPC、这些 NPC 的生命状态 / 行动状态和所有建筑外部状态，不含公告牌通告、参考日程或备注；进入可进入建筑时，该快照还包含建筑内 NPC 的生命状态和行动状态。对已经在场的 NPC，进入 / 离开事件本身就是人员变化信息，不再额外广播完整 `people_present` 或完整 `people_statuses`。除进入者的一次性快照外，后续地点/建筑状态见闻应使用字段级差量，例如只记录受损、升级或某个工位占用变化；公告牌两页则由守备官发布时的独立全站事件传递。

```json
{
  "id": "plaza",
  "name": "广场",
  "kind": "plaza",
  "people_present": ["stableman_01", "veteran_deputy_01"],
  "people_statuses": [
    {"npc_id": "stableman_01", "life_status": "healthy", "life_status_text": "健康", "action_status": "待命"},
    {"npc_id": "veteran_deputy_01", "life_status": "injured", "life_status_text": "受伤", "action_status": "协助修复围墙"}
  ],
  "current_notice": "今晚在主厅前集合。",
  "current_public_note_ids": [],
  "state_snapshot": {
    "building_external_states": {
      "main_hall": {"level": 1, "condition": "intact", "is_enterable": false, "operational_efficiency": 1.0},
      "chapel": {"level": 1, "condition": "intact", "is_enterable": true, "operational_efficiency": 1.0},
      "wall": {"level": 1, "condition": "damaged", "is_enterable": false, "operational_efficiency": 0.75}
    }
  }
}
```

## 22.5 Crafting Recipe

```json
{
  "id": "craft_sword_shield",
  "building_id": "blacksmith",
  "output_item_id": "item_sword_shield",
  "output_amount": 1,
  "stages": [
    {"id": "forge_blade", "name": "锻刃", "cost": {"iron": 1}},
    {"id": "shape_shield", "name": "制盾", "cost": {"wood": 1}},
    {"id": "forge_fittings", "name": "锻造配件", "cost": {"iron": 1}},
    {"id": "assemble_finish", "name": "装配打磨", "cost": {"iron": 1}}
  ]
}
```

## 22.6 Horse Runtime

```json
{
  "horse_id": "horse_chestnut_wind",
  "name": "栗风",
  "growth": 0.6,
  "is_adult": true,
  "hp": 76.0,
  "base_hp": 76.0,
  "natural_max_hp": 76.0,
  "care_bonus_hp": 0.0,
  "care_bonus_cap": 0.0,
  "extra_hp": 0.0,
  "extra_hp_cap": 0.0,
  "satiety": 80.0,
  "max_satiety": 80.0,
  "breeding_probability": 0.0,
  "breeding_cooldown_remaining_seconds": 0.0,
  "breeding_cooldown_active": false,
  "location": "stable",
  "feeding": {"active": false, "elapsed_seconds": 0.0},
  "assigned_npc_id": "",
  "ridden_by_npc_id": ""
}
```

马匹自然上限由成长值和配置计算，`base_hp / extra_hp / extra_hp_cap` 由权威总 HP 与照料额外 HP 派生，`is_adult` 与冷却活动状态也由程序派生；程序不得相信 UI 或 LLM 传入这些值。配置另行保存初始马、饱食消耗、进食、自愈、成长、繁育概率增量 / 上限、繁育冷却和额外 HP 培养参数。

---

# 23. 下一步建议

## 23.1 第二阶段：Godot 驿站基础场景

实现：

- 低模地图
- NPC 移动
- 建筑点击
- 资源栏
- 时间流逝
- 基础工作产出

## 23.2 第三阶段：AI 与经营接入

实现：

- 每日计划
- 玩家对话
- NPC 主动找玩家交涉
- 征召状态
- 已入伍 NPC 指令撰写、发布与计划重估
- 首次睡眠总结

## 23.3 第四阶段：战斗与结算

实现：

- 警铃
- 集结
- 敌人攻击
- 武器策略
- 血量 30% 判定
- 昏迷、治疗与复苏
- 战场公开信息写入
- 战后事件与结局

---

# 24. 暂定宣传文案

> 这不是骑士的史诗，也不是国王的战争。  
> 这里没有正规军，只有一个边境驿站、八个本不该上战场的人，以及一纸迟来的军令。  
> 你可以说服他们、收买他们、欺骗他们，也可以威胁他们。  
> 他们会拿起武器，也会记住你说过的话。  
> 当敌人攻到门口时，每个人都会问自己：这到底是不是我的战争？
