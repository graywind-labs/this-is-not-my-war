# 3D Runtime Assets

正式运行时 3D 文件。首选 `.glb`，按 `buildings/`、`props/`、`characters/`、`animations/`、`nature/` 和 `equipment/` 分类。第三方原始来源与工作文件不放在这里。

T0130-P0–P8 因 Synty Source Files 只提供 FBX，允许经来源审计且已逐人验收的运行时 FBX 留在 `synty/t0130_pilot/`；对应 FBX 已纳入 Git LFS。P5–P8 候选比较目录已移出运行时，只保留选中的 ShieldMaiden、Wizard 长袍基础、Pirates GovDaughter 与 Pirates Firstmate。KayKit 动作继续以 GLB 放在 `kaykit/animations/rig_medium/`。完整包、Maya / OBJ 源和未筛选文件不得进入本目录。
