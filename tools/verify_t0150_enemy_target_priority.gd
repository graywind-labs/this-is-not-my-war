# T0150's layered priority/retaliation contract was explicitly replaced by
# T0196's single-radius presence lock. Keep the historical entry point as an
# alias so full regression runners execute the current authoritative contract.
extends "res://tools/verify_t0196_unified_enemy_targeting.gd"
