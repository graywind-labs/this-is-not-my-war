【兼容占位，不参与运行】

`ModelAdapter` 已把历史 call_type `dialogue_plan_revision_judgement` 与当前
`plan_revision_judgement` 统一映射到 `plan_revision_judgement_system_prompt.txt`。
当前正式范围判别同时支持对话与行动失败，并读取统一 NPC 上下文、全量紧凑短期记忆、
驿站状态、行动候选、建筑 / 资源事实和完整当前计划。

保留本文件只为解释旧文件名；不得重新接入此前“轻量调用、不注入长短期记忆”的旧合同。
